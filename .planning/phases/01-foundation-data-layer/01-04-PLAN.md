---
phase: 01-foundation-data-layer
plan: 4
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: []
autonomous: true
requirements: [DATA-01, DATA-03, DATA-04]
user_setup: []

must_haves:
  truths:
    - "Running the InitialSchema migration against a fresh docker-compose Postgres 16 produces the schema, and `migration:generate` immediately after reports NO changes (empty diff) — proving entities and migration agree (DATA-01 success criterion 1)."
    - "The game_question_outcome reference rows (fizzles/danger/shot/goal, ladder 1-4) exist after migration (DATA-01 success criterion 2)."
    - "The app boots against the docker Postgres 16 with enableShutdownHooks() active and exits promptly on SIGTERM (DATA-04 success criterion 3)."
    - "Re-running `npm run migration:run` after InitialSchema has applied is a no-op — TypeORM records applied migrations in its migrations table and runs each migration inside a transaction, so an interrupted run rolls back with no partial schema."
    - statement: "Concurrent CLI migration:run and app boot cannot corrupt the schema — the shared DataSource sets synchronize:false so the app never mutates schema on connect, and migration:run applies InitialSchema inside a transaction."
      verification: backstop
  artifacts: []
  key_links:
    - "migration:generate empty-diff is the DATA-01 gate — a non-empty diff mentioning uq_game_fixture_live or uq_gq_one_open_per_game triggers the Pitfall 1 fallback (match TypeORM's emitted WHERE-clause text in the entity @Index metadata)."
---

<objective>
Prove the phase end-to-end against a real Postgres 16: push the schema (the mandatory [BLOCKING] migration run), assert the empty-diff gate and the seed rows, and confirm the app boots with shutdown hooks. This closes DATA-01 (verification), DATA-03 (shared DataSource works for the CLI), and DATA-04 (boot).

Purpose: Build/type checks pass without a live DB (types come from config, not the database), so the phase can only be truly verified by running the migration and generating a diff against the migrated database. This plan is that verification.
Output: A migrated Postgres 16 whose `migration:generate` diff is empty, 4 seed rows present, and a clean boot+shutdown.

## Artifacts this phase produces (this plan)
- No new source files (verification-only). Produces the applied database schema state and the empty-diff proof.

<flagged_assumptions>
- [FLAGGED ASSUMPTION — edge probe DATA-04 "unclassified", unresolved] The edge probe did not classify enableShutdownHooks behavior under concurrent/interrupted shutdown. Working assumption: a single SIGTERM triggers Nest's onApplicationShutdown once, closing the TypeORM pool idempotently; a second signal during shutdown does not corrupt state. Confirm during execution; do not silently drop.
</flagged_assumptions>
</objective>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-02-PLAN.md
@.planning/phases/01-foundation-data-layer/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push schema — bring up Postgres 16 and run the migration</name>
  <files>docker-compose.yml (read-only), apps/gutcallfun-core/.env (local, git-ignored — add DATABASE_URL if absent), live Postgres 16 schema (applied, not a tracked file)</files>
  <read_first>
    - docker-compose.yml (repo root — db service, port 127.0.0.1:5488, healthcheck fixed in plan 01)
    - apps/gutcallfun-core/.env.example (canonical DATABASE_URL shape from plan 01)
    - apps/gutcallfun-core/package.json (migration:run script — plan 01)
    - apps/gutcallfun-core/src/db/data-source.ts (connection target — plan 02)
    - apps/gutcallfun-core/src/db/migrations/InitialSchema.ts (the migration to apply — plan 03)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Environment Availability: use `docker compose` V2 syntax, psql 16 present; Pitfall 5)
  </read_first>
  <action>
    This is the mandatory [BLOCKING] schema-push step — the phase cannot pass verification without it (build/type checks pass without a live DB, so a false-positive verification is possible otherwise).
    Ensure the local `apps/gutcallfun-core/.env` (git-ignored, local only — it already holds TxLINE creds) contains a `DATABASE_URL` pointing at the docker db; if the key is absent, ADD (do not overwrite the file) `DATABASE_URL=postgresql://gutcallfun:changeme-local-only@127.0.0.1:5488/gutcallfun` plus `PORT=3000` and `NODE_ENV=development`. Never commit `.env`.
    From the repo root, start Postgres: `docker compose up -d db` (V2 space syntax — the hyphenated binary is not installed). Wait for the db service to report healthy (`docker compose ps` / the healthcheck fixed in plan 01).
    From `apps/gutcallfun-core`, run `npm run migration:run`. Confirm it applies `InitialSchema{timestamp}` with exit 0.
    Then assert the seed rows via psql: connect to the db (`psql postgresql://gutcallfun:changeme-local-only@127.0.0.1:5488/gutcallfun`) and confirm `SELECT count(*) FROM game_question_outcome;` returns 4 and `SELECT key, ladder_position FROM game_question_outcome ORDER BY ladder_position;` returns fizzles/1, danger/2, shot/3, goal/4.
  </action>
  <verify>
    <automated>cd /Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun && docker compose up -d db && sleep 3 && cd apps/gutcallfun-core && npm run migration:run && psql postgresql://gutcallfun:changeme-local-only@127.0.0.1:5488/gutcallfun -tAc "SELECT count(*) FROM game_question_outcome;" | grep -q '^4$' && echo SEEDED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows the db service healthy.
    - `npm run migration:run` exits 0 and reports InitialSchema applied.
    - `psql ... -tAc "SELECT count(*) FROM game_question_outcome"` returns `4`.
    - The 4 outcome rows have ladder positions fizzles=1, danger=2, shot=3, goal=4.
    - `psql ... "\d game_question"` shows the partial unique index `uq_gq_one_open_per_game`; `\d game` shows `uq_game_fixture_live`.
  </acceptance_criteria>
  <done>Postgres 16 is up, the InitialSchema migration is applied, and the 4 locked outcome rows plus both partial indexes exist in the live database.</done>
</task>

<task type="auto">
  <name>Task 2: Empty-diff gate + boot with shutdown hooks</name>
  <files>apps/gutcallfun-core/src/db/migrations/ (transient check-migration file — must be deleted), apps/gutcallfun-core/dist/main.js (built artifact, boot target)</files>
  <read_first>
    - apps/gutcallfun-core/package.json (migration:generate, start scripts)
    - apps/gutcallfun-core/src/main.ts (enableShutdownHooks — plan 02)
    - apps/gutcallfun-core/src/models (entities that must diff empty against the migrated DB — plan 03)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Open Question 1 empty-diff gate, Pitfall 1 partial-index fallback, Pitfall 4 shutdown hooks)
  </read_first>
  <action>
    Empty-diff gate (DATA-01): from `apps/gutcallfun-core`, run `npm run migration:generate` against the already-migrated DB. TypeORM 0.3.x prints `No changes in database schema were found - cannot generate a migration` and exits non-zero WHEN THERE IS NO DIFF — that "no changes" outcome is the success condition here. Capture the output and confirm it contains `No changes in database schema were found`. If instead a migration file is generated mentioning `uq_game_fixture_live` or `uq_gq_one_open_per_game`, apply the Pitfall 1 fallback: adjust the entity `@Index` `where` text to exactly match TypeORM's emitted form, delete the spurious generated file, and re-run until empty. Delete any transient generated check file so it is not committed.
    Boot + shutdown (DATA-04): build (`npm run build`) and start the app against the docker db (`node dist/main` with DATABASE_URL set, or `npm run start:prod`). Confirm it logs listening on the configured PORT (no ECONNREFUSED — a clear boot proves config validation passed and the DB connection opened). Then send SIGTERM to the process and confirm it exits promptly (within a few seconds — enableShutdownHooks fires onApplicationShutdown), not after the full kill grace period. Also confirm the negative path: temporarily unset DATABASE_URL and confirm boot aborts with the class-validator error (fail-fast), not a raw ECONNREFUSED — then restore DATABASE_URL.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build >/dev/null 2>&1 && MAIN="$PWD/dist/main.js" && (npm run migration:generate 2>&1 || true) | grep -q 'No changes in database schema were found' && D="$(mktemp -d)" && { ( cd "$D" && env -u DATABASE_URL node "$MAIN" ) >/tmp/gc_failfast.log 2>&1 & FF=$!; ( sleep 8; kill -9 $FF 2>/dev/null ) & WD=$!; wait $FF 2>/dev/null; kill -9 $WD 2>/dev/null; ! grep -q 'successfully started' /tmp/gc_failfast.log && ! grep -qi 'Cannot find module' /tmp/gc_failfast.log; } && echo VERIFY_OK
  </verify>
  <acceptance_criteria>
    - `npm run migration:generate` output contains `No changes in database schema were found` (empty diff — entities and migration agree), OR any spurious diff was resolved via the Pitfall 1 fallback and a re-run is empty.
    - No transient check-migration file remains under `src/db/migrations/` after the gate.
    - `node dist/main` (with DATABASE_URL set) logs the app listening on PORT with no ECONNREFUSED.
    - Sending SIGTERM to the running app causes it to exit within a few seconds (shutdown hooks active), not the full grace-period timeout.
    - Booting with DATABASE_URL unset aborts with a class-validator/env error (fail-fast), not a raw Postgres connection error.
  </acceptance_criteria>
  <done>migration:generate reports an empty diff, no stray migration file remains, the app boots cleanly against Postgres 16 and shuts down promptly on SIGTERM, and a missing DATABASE_URL fails fast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI migration runner → live schema | Migration application crosses into the database |
| app process → Postgres connection | Runtime connection + graceful teardown surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | schema drift (entities vs migration) | high | mitigate | migration:generate empty-diff gate proves the hand-authored migration and the entities agree — a non-empty diff blocks the phase (DATA-01) |
| T-01-11 | DoS | ungraceful shutdown | medium | mitigate | SIGTERM exit-within-seconds check confirms enableShutdownHooks closes the pool (DATA-04) |
| T-01-04 | Information Disclosure | Postgres port | medium | accept | db bound to 127.0.0.1:5488 (loopback) per plan 01; local dev only, no external exposure — accepted for the local docker-compose target |
</threat_model>

<verification>
- `npm run migration:run` exit 0; seed count = 4; both partial indexes present in `\d` output.
- `npm run migration:generate` → `No changes in database schema were found`.
- `node dist/main` boots (no ECONNREFUSED); SIGTERM → prompt exit; missing DATABASE_URL → fail-fast error.
</verification>

<success_criteria>
The migration applies to a fresh Postgres 16 producing an empty `migration:generate` diff, the 4 locked outcome rows exist, and the app boots with shutdown hooks active and fails fast on missing config — DATA-01, DATA-03, and DATA-04 verified end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-04-SUMMARY.md` when done.
</output>
