---
phase: quick-260718-48a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.spec.ts
  - apps/gutcallfun-core/src/modules/ingest/stream/upstream.ts
  - apps/gutcallfun-core/src/modules/ingest/stream/stream-manager.service.ts
  - apps/gutcallfun-core/src/modules/ingest/stream/upstream.spec.ts
  - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-UAT.md
autonomous: true
requirements: [GAME-01, INGST-02, INGST-04]

must_haves:
  truths:
    - "A fixtures-cron tick over a currently-live game never rewrites that game's status, scoreP1, scoreP2, streamCursor, streamCursorAt or currentStatusId."
    - "The fixtures cron still creates new rows (with status) and still refreshes metadata (startsAt, team names, competition, participant ids) on existing rows."
    - "StreamManagerService.stop(gameId) / onApplicationShutdown() interrupts an actively-receiving SSE stream promptly, without waiting for the 30s idle watchdog."
    - "The abort listener and the idle-watchdog timer registered by runSingleStream are released on every exit path — normal stream end, error, and abort."
    - "The stream-manager doc comment describes the actual abort mechanism (signal into fetch + reader.cancel on abort), not a claim that is false as implemented."
    - "All previously-green tests (138 unit + 2 e2e) still pass, and `npm run build` succeeds."
  artifacts:
    - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts
    - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.spec.ts
    - apps/gutcallfun-core/src/modules/ingest/stream/upstream.ts
    - apps/gutcallfun-core/src/modules/ingest/stream/stream-manager.service.ts
    - apps/gutcallfun-core/src/modules/ingest/stream/upstream.spec.ts
  key_links:
    - "SingleStreamOpts.signal ← connectWithRetry's opts.signal ← StreamManagerService's per-game AbortController.signal"
    - "SingleStreamOpts.signal → fetchImpl init.signal (transport-level abort)"
    - "SingleStreamOpts.signal → 'abort' listener → reader.cancel() (breaks a blocked read loop)"
    - "runSingleStream finally block → removeEventListener('abort', onAbort) + clearTimeout(watchdogTimer)"
    - "FixturesCronService existing-row branch → Repository.update({id}, {8 metadata columns only})"
---

<objective>
Fix the two BLOCKER findings from the Phase 2 code review (`02-REVIEW.md`):

- **CR-01** — `FixturesCronService.upsertAll()` does a read-modify-write via a
  full-entity `Repository.save()`, which races with `SourceSchedulerService`'s
  atomic status claim and `EventIngestService`'s per-event score/cursor writes
  and can silently revert them on a currently-live game.
- **CR-02** — `connectWithRetry` never threads its caller's `AbortSignal` into
  the actual `fetch()` call or the `reader.read()` loop, so `stop()` /
  `onApplicationShutdown()` does not interrupt an actively-receiving stream
  (SIGTERM hang risk, duplicate-connection leak). `stream-manager.service.ts`
  documents an invariant that is currently false.

Purpose: remove a silent live-state-corruption race and make the INGST-04
shutdown seam actually work, closing the code-review blockers before the
feature freeze.

Output: two source fixes, one corrected doc comment, three new regression
tests, updated existing cron spec, and an 02-UAT.md note on item #2.
</objective>

<scope_boundaries>
EXACTLY CR-01 and CR-02. The five WARNING findings (WR-01…WR-05) and both INFO
findings (IN-01, IN-02) in `02-REVIEW.md` are explicitly OUT OF SCOPE — do not
touch `event-ingest.service.ts`, `message-normalizer.ts`, `replay.ts`,
`txline-fixtures.client.ts`, the registry `remove()` methods, or
`connectUpstream()` (the one-shot variant at `upstream.ts:177-269`).
</scope_boundaries>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/workstreams/backend/STATE.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-REVIEW.md
@apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts
@apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.spec.ts
@apps/gutcallfun-core/src/modules/ingest/stream/upstream.ts
@apps/gutcallfun-core/src/modules/ingest/stream/stream-manager.service.ts
@apps/gutcallfun-core/src/modules/ingest/stream/stream-manager.service.spec.ts
</context>

<interface_context>
Facts the executor needs, already verified during planning — do not re-derive:

- `ConnectWithRetryOptions.fetchImpl?: (url: string, opts: RequestInit) => Promise<Response>`
  (`upstream.ts:317`). `RequestInit` already carries an optional `signal`, so no
  type widening is needed to pass one.
- `connectWithRetry` accepts test overrides: `watchdogMs`, `baseBackoffMs`,
  `capBackoffMs`, `jitterMs`, `signal`. Use these instead of fake timers.
- `runSingleStream` is module-private and NOT exported. All CR-02 tests must
  drive it through the exported `connectWithRetry` with an injected `fetchImpl`.
- `stream-manager.service.spec.ts` does `jest.mock('./upstream', ...)` at module
  scope, so the real `connectWithRetry` is unavailable there. CR-02 tests belong
  in a NEW file `src/modules/ingest/stream/upstream.spec.ts` (none exists today).
- Jest config: `rootDir: "src"`, `testRegex: ".*\\.spec\\.ts$"`. Run a single
  spec with a positional path-regex, e.g.
  `npm test -- modules/ingest/stream/upstream.spec` from `apps/gutcallfun-core`.
- `runSingleStream` already has a `try { … } finally { if (watchdogTimer !== undefined) clearTimeout(watchdogTimer); }`
  around the read loop (`upstream.ts:469,531-533`) — the abort-listener removal
  is added to that EXISTING finally block, not a new one.
- The existing cron spec's `buildRepoMock` (`fixtures-cron.service.spec.ts:28-35`)
  only stubs `findOne`/`create`/`save`. Adding a partial-update path requires an
  `update` jest.fn on that mock, and Test 1's `expect(repo.save).toHaveBeenCalledTimes(2)`
  assertion will no longer hold — it must be re-expressed as one `save` (insert)
  plus one `update`.
- Planner pre-check (CONFIRM in Task 2, do not assume): a grep of the reference
  file `/Users/maximkravchuk/Development/Projects/Solana/txodds_hackaton/txodds-latency-test-app/src/upstream.ts`
  shows `signal` appearing only in the retry-loop guard and the `sleep()` helper
  — the same gap. If that holds, this fix is an improvement OVER the reference,
  and the SUMMARY must say so explicitly.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — replace the fixtures-cron full-entity write with a targeted partial update</name>
  <files>
    apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts
    apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.spec.ts
  </files>
  <behavior>
    - Test A (new, the CR-01 regression): a live game already in the DB is
      re-discovered by `fetchPast` on a cron tick. Seed the repo mock's existing
      row with `status: GameStatus.LIVE`, `scoreP1: 2`, `scoreP2: 1`,
      `streamCursor: '4711'`, `currentStatusId: 2`. After `discoverFixtures()`:
      * `repo.update` was called once for that row, keyed by `{ id: <existing id> }`;
      * `Object.keys(updatePayload).sort()` equals exactly
        `['competition','fixtureGroupId','participant1Id','participant1IsHome','participant2Id','startsAt','team1Name','team2Name']`
        — an exact-key assertion, so any future re-addition of an owned-elsewhere
        column fails the test;
      * therefore the payload carries no `status`, no `scoreP1`/`scoreP2`, no
        `streamCursor`/`streamCursorAt`, no `currentStatusId`;
      * `repo.save` was NOT called for the existing row (only ever for inserts).
    - Test B (new): the INSERT path is unchanged — a fixture with no existing row
      still goes through `repo.create` + `repo.save` and its payload DOES carry
      `status` (first-seen rows are the only place this cron sets status).
    - Test 1 (existing, must be updated, not deleted): "upserts by fixture_id"
      now asserts one `repo.save` (the insert) and one `repo.update` (the
      existing row) rather than two saves.
    - Tests 2, 3, 4 (existing) must keep passing; Test 2 and 3 exercise
      insert-only paths and Test 4 is fetch-argument-only, so only the mock
      helper needs the new `update` stub.
  </behavior>
  <action>
    Extend `buildRepoMock` in the spec with an `update: jest.fn(async () => ({ affected: 1 }))`
    stub and include it in the returned object, then write Test A and Test B and
    rewrite Test 1's assertions per the behavior block above.

    In `fixtures-cron.service.ts`, rework the existing-row branch of `upsertAll()`
    so that instead of mutating the loaded entity and writing it back in full, it
    issues a targeted partial write: `await this.gameRepo.update({ id: existing.id }, { … })`
    carrying ONLY the eight metadata columns this cron owns — `startsAt` (as
    `new Date(fixture.StartTime)`), `team1Name`, `team2Name`, `competition`,
    `fixtureGroupId`, `participant1Id`, `participant2Id`, `participant1IsHome` —
    then `continue`. Leave the insert branch (`create` + `save`) exactly as-is.

    Root cause being fixed (state it in the code comment): TypeORM's `save()` on
    a loaded entity emits an UPDATE for every column using values captured at
    `findOne()` time, so a concurrent write from `SourceSchedulerService` (the
    atomic live-claim) or `EventIngestService` (per-event score/cursor flush)
    landing between the read and the write gets silently reverted. A partial
    UPDATE touching only cron-owned columns removes the read-then-write race
    without needing a transaction or row lock, because no other writer touches
    those columns.

    Keep and expand the existing head comment above `upsertAll()` explaining
    that status is deliberately set on INSERT only and never on the update path
    (Plan 02-04 property) so this cron can never revert a live game back to
    scheduled — and add that the partial-update form now also protects the
    denormalized score and stream cursor. Do NOT reintroduce any owned-elsewhere
    column into the update payload.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm test -- modules/ingest/fixtures/fixtures-cron.service.spec</automated>
    <automated>cd apps/gutcallfun-core && grep -n 'gameRepo.update' src/modules/ingest/fixtures/fixtures-cron.service.ts</automated>
  </verify>
  <done>
    The cron's existing-row branch performs a partial update over exactly eight
    metadata columns; the exact-key regression test proves status/score/cursor
    are untouched; the insert path still sets status; all four pre-existing cron
    tests pass with updated assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-02 — cross-check the reference, then thread the AbortSignal into the SSE fetch and read loop</name>
  <files>
    apps/gutcallfun-core/src/modules/ingest/stream/upstream.ts
    apps/gutcallfun-core/src/modules/ingest/stream/stream-manager.service.ts
  </files>
  <action>
    FIRST, read the reference port source at
    `/Users/maximkravchuk/Development/Projects/Solana/txodds_hackaton/txodds-latency-test-app/src/upstream.ts`
    and determine whether its own single-stream function threads an AbortSignal
    into its `fetch()` init object and/or its reader loop. Record the answer —
    it drives one line of the SUMMARY:
      * if the reference DOES thread it → our port dropped it; restore the
        reference's approach and say so;
      * if the reference does NOT (planner's grep suggests this) → this fix is a
        genuine improvement OVER the reference, and the SUMMARY must state that
        explicitly so a future contributor doesn't "restore fidelity" by
        reverting it.

    THEN, in `upstream.ts`:
    1. Add an optional `signal?: AbortSignal` field to the private
       `SingleStreamOpts` interface, documented as the caller's stop/shutdown
       seam (INGST-04).
    2. In `runSingleStream`, pass `signal: opts.signal` through in the init
       object handed to `opts.fetchImpl` alongside the existing `method` and
       `headers`, so the transport itself is abortable.
    3. After `const reader = response.body.getReader();`, define an abort handler
       that calls `reader.cancel()` inside a try/catch that swallows errors (the
       reader may already be closed), and register it with
       `opts.signal?.addEventListener('abort', handler, { once: true })`. Keep a
       reference to the handler so it can be removed later.
    4. In the EXISTING `finally` block that already clears the watchdog timer,
       also call `opts.signal?.removeEventListener('abort', handler)` — so
       neither the listener nor the timer leaks on ANY exit path: normal stream
       end, thrown error (including the AUTH_EXPIRED sentinel), or abort.
    5. In `connectWithRetry`, pass `signal` through in the object literal handed
       to `runSingleStream(...)`, next to the existing `watchdogMs` /
       `onLastEventId` / `onConnected` / `onConnectInfo` fields.

    Do NOT change the one-shot `connectUpstream()` variant (IN-02, out of scope).
    Preserve every ported behavior untouched: the AUTH_EXPIRED sentinel and its
    no-retry contract, the idle watchdog and its reset-on-every-raw-chunk rule,
    exponential backoff with jitter, Last-Event-ID resume seeding and carry-over,
    and above all the receivedAt measurement gate — `const receivedAt = Date.now()`
    must remain the first statement after `reader.read()` resolves, with no new
    work inserted before it.

    THEN, in `stream-manager.service.ts`, rewrite the second paragraph of the
    `onApplicationShutdown` doc block — the sentence asserting that a blocked
    read throws immediately once its controller aborts. That claim was false as
    implemented and is only true after this change, so describe the actual
    two-part mechanism now in place: the per-game controller's signal is threaded
    into the SSE `fetch()` so the transport aborts, and an abort listener cancels
    the active stream reader so a blocked read resolves at once rather than
    waiting out the 30s idle watchdog. Reference CR-02 in a short parenthetical
    so the history is traceable. Leave the INGST-04 cursor-flush paragraph below
    it unchanged — that argument is still correct.

    Never log or interpolate the JWT or API token anywhere in this change (INGST-01).
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build</automated>
    <automated>cd apps/gutcallfun-core && npm test -- modules/ingest/stream/stream-manager.service.spec</automated>
  </verify>
  <done>
    `SingleStreamOpts` carries `signal`; the fetch init object and an abort-driven
    `reader.cancel()` both consume it; the shared finally block releases both the
    listener and the watchdog timer; `connectWithRetry` forwards `signal`; the
    stream-manager doc comment describes the real mechanism; the build passes and
    the existing stream-manager suite is still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-02 regression tests, UAT note, and full-suite verification</name>
  <files>
    apps/gutcallfun-core/src/modules/ingest/stream/upstream.spec.ts
    .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-UAT.md
  </files>
  <behavior>
    New file `upstream.spec.ts`, driving the REAL exported `connectWithRetry`
    with an injected `fetchImpl` (no `jest.mock('./upstream')` here). Build a
    controllable fake: a `Response`-shaped object with `status: 200`, `ok: true`,
    `headers: { get: () => null }`, and a `body.getReader()` returning a fake
    reader whose `read()` hands back a promise the test holds open, and whose
    `cancel()` is a jest.fn that resolves that pending promise with
    `{ done: true }` (mirroring a real reader, where cancel makes pending reads
    resolve as done). Use `baseBackoffMs: 1, capBackoffMs: 1, jitterMs: 0` so the
    retry loop never stalls the test — no fake timers needed.

    - Test 1 (signal reaches the transport): assert the init object captured by
      `fetchImpl` has a `signal` that is the very `AbortSignal` passed into
      `connectWithRetry`.
    - Test 2 (prompt abort, not watchdog): pass `watchdogMs: 60_000` and a reader
      whose `read()` never settles on its own. Await the connection opening, then
      `controller.abort()`. Assert the `connectWithRetry` promise resolves and
      that `reader.cancel` was called, with elapsed wall time under ~1s. The
      60s watchdog makes this unambiguous: only the abort path could have
      interrupted the blocked read that fast.
    - Test 3 (no watchdog-timer leak on normal completion): pass
      `watchdogMs: 50`. Drive the reader to resolve `{ done: true }` so the
      stream ends normally, and abort from the `onMeta` handler when the
      `reconnecting` meta fires so the retry loop exits. After the promise
      resolves, snapshot `reader.cancel.mock.calls.length`, wait ~150ms of real
      time, and assert the count did NOT increase — proving the watchdog timer
      was cleared in the finally block rather than firing after the stream ended.
    - Test 4 (no abort-listener leak): spy on the controller's signal via
      `jest.spyOn(controller.signal, 'addEventListener')` and
      `jest.spyOn(controller.signal, 'removeEventListener')`. After a normally-
      completed stream, assert the exact handler function registered by
      `runSingleStream` was later passed to `removeEventListener`. Compare the
      captured handler identity, not just the call count, since the `sleep()`
      helper also registers an abort listener on the same signal.
    - Regression guard: assert the AUTH_EXPIRED path still short-circuits — a
      `fetchImpl` returning `status: 401` yields exactly one
      `{ type: 'auth_expired' }` meta and `connectWithRetry` returns without a
      second fetch attempt.
  </behavior>
  <action>
    Write the tests above. Keep every fake credential value obviously synthetic
    (e.g. `'jwt-test'`, `'token-test'`) and never read real values from `.env`
    (INGST-01).

    Then update
    `.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-UAT.md`
    item #2 ("Genuinely-blocked socket abort within SIGTERM grace"): append a
    dated note recording that CR-02 is fixed — the signal now reaches both the
    SSE fetch and the reader, and the previously-false doc claim has been
    corrected — while stating honestly that the automated coverage still mocks
    the fetch layer, so observing a real OS-level stalled socket abort remains a
    human verification step. Leave `result: [pending]` as-is; do not flip it and
    do not touch the Summary counts.

    Finally run the full verification sweep from `apps/gutcallfun-core`:
    `npm run build`, `npm test` (full unit suite — was 138 green), and
    `npm run test:e2e` (was 2 green; Postgres 16 is up on 127.0.0.1:5488 and
    `.env` is configured). Every previously-passing test must still pass; the
    only expected count change is the tests added by Tasks 1 and 3.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build</automated>
    <automated>cd apps/gutcallfun-core && npm test</automated>
    <automated>cd apps/gutcallfun-core && npm run test:e2e</automated>
  </verify>
  <done>
    `upstream.spec.ts` exists and proves signal-into-fetch, prompt abort of a
    blocked read, watchdog-timer cleanup, abort-listener cleanup, and the intact
    AUTH_EXPIRED short-circuit. 02-UAT.md item #2 carries the CR-02 note. Build
    passes; the full unit suite and both e2e specs are green with no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TxLINE SSE origin → ingest process | Untrusted remote stream bytes cross into the read loop |
| Cron writer ↔ stream/scheduler writers → `game` row | Concurrent writers on shared mutable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48a-01 | Tampering | `FixturesCronService.upsertAll` | high | mitigate | Partial `update()` over eight cron-owned metadata columns removes the read-modify-write race that lets a stale snapshot overwrite live status/score/cursor (Task 1) |
| T-48a-02 | Denial of Service | `runSingleStream` / `StreamManagerService` | high | mitigate | Signal into `fetch()` + abort-driven `reader.cancel()` bounds shutdown latency and prevents duplicate orphaned SSE connections per game after stop()-then-start() (Task 2) |
| T-48a-03 | Information Disclosure | new specs + doc comments | medium | mitigate | Only synthetic credential literals in tests; no JWT/API-token value is logged or interpolated anywhere in this change (INGST-01) |
| T-48a-04 | Denial of Service | abort listener / watchdog timer | medium | mitigate | Shared finally block releases both the listener and the timer on every exit path; Tests 3 and 4 assert no leak (Task 3) |
| T-48a-SC | Tampering | package installs | low | accept | No new dependencies are added by this change — nothing to audit |
</threat_model>

<verification>
1. `cd apps/gutcallfun-core && npm run build` — compiles clean.
2. `npm test` — full unit suite green; ≥138 tests, no previously-passing test regressed.
3. `npm run test:e2e` — both e2e specs green against Postgres 16 on 127.0.0.1:5488.
4. The fixtures-cron existing-row path issues `update`, never a full-entity write,
   and its payload keys are asserted exactly.
5. `connectWithRetry` forwards its signal into `runSingleStream`, which uses it
   for both the fetch init and reader cancellation.
6. `stream-manager.service.ts` no longer asserts an invariant it does not implement.
7. No JWT or API token value appears in any new log line, comment, or test fixture.
</verification>

<success_criteria>
- CR-01 and CR-02 are both closed; WR-01…WR-05, IN-01 and IN-02 remain untouched.
- A concurrent live-game write can no longer be reverted by a cron tick.
- An active SSE stream is interrupted by abort in well under the 30s watchdog window.
- No abort-listener or timer leaks on normal end, error, or abort.
- Every ported upstream behavior (AUTH_EXPIRED, watchdog, backoff, Last-Event-ID
  resume, receivedAt-before-decode ordering) is preserved.
- The reference cross-check verdict is recorded explicitly in the SUMMARY.
- Build green, 138+ unit tests green, 2 e2e green.
</success_criteria>

<output>
Create `.planning/quick/260718-48a-fix-cr-01-fixtures-cron-write-race-and-c/260718-48a-SUMMARY.md` when done.

The SUMMARY must explicitly state the reference cross-check verdict from Task 2:
whether `/Users/maximkravchuk/Development/Projects/Solana/txodds_hackaton/txodds-latency-test-app/src/upstream.ts`
threads an AbortSignal into its fetch/read loop, and therefore whether this change
restores dropped fidelity or improves on the reference.
</output>
