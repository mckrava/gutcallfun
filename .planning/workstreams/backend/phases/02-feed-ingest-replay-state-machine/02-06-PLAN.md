---
phase: 02-feed-ingest-replay-state-machine
plan: 06
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - apps/gutcallfun-core/src/modules/ingest/replay/replay.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/historical.client.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/replay-source.service.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/replay-starter.service.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/replay.module.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/replay-starter.service.spec.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/replay.spec.ts
  - apps/gutcallfun-core/scripts/replay-create.ts
  - apps/gutcallfun-core/package.json
autonomous: true
requirements: [RPLY-01, RPLY-02, RPLY-03]

must_haves:
  truths:
    - "The replay emitter feeds the identical downstream pipeline as live: it calls the SAME EventIngestService.processEvent(gameId, raw) entry point, with NO downstream mode flag (RPLY-02)."
    - "Timestamps are rebased: delta = now() − firstEvent.Ts is added to every event's Ts before emission; the live path is an untouched passthrough (D-03, RPLY-01)."
    - "Pacing follows original inter-event gaps ÷ speed (default speed=1); the pre-match segment plays at FULL original pacing with NO compression/skip — the reference repo's maxGapMs=5000 clamp is explicitly overridden/removed (D-04; RESEARCH Summary conflict resolution — CONTEXT wins over port-as-is)."
    - "Two recorded sources feed one source-agnostic emitter: Source A = historical fetch GET /api/scores/historical/{fixtureId} (cached in-memory per process); Source B = captured NDJSON files / the game's own game_event rows as an ordered raw-event array (D-01). Source B is the same code path reused by boot recovery (Plan 07)."
    - "Replay start auto-wipes that game's prior game_event rows and resets denormalized score/state BEFORE emitting, guarded to is_replay=true games ONLY, so re-flipping the same row for take #2 never collides with UNIQUE(game_id, seq) (D-07, RPLY-03 idempotency)."
    - "An admin/dev action creates a replay game via `npm run replay:create -- <fixtureId> [--speed N]`: is_replay=true, starts_at=now()+~3min, supplied names; multiple takes produce multiple replay rows (partial unique index uq_game_fixture_live permits this) (D-06, D-08, RPLY-03)."
    - "speed>1 is reachable ONLY via the script arg / headless config, never from a user-facing surface (D-04)."
    - "If Source A returns empty (fixture outside the 2wk–6h retention window), the emitter falls back to Source B (D-01; RESEARCH Pitfall 2)."
    - statement: "Replay pacing preserves original inter-event gap ratios at speed=1 so event time aligns with wall time (RPLY-03 precision edge)."
      verification: backstop
  artifacts:
    - apps/gutcallfun-core/src/modules/ingest/replay/replay.ts
    - apps/gutcallfun-core/src/modules/ingest/replay/historical.client.ts
    - apps/gutcallfun-core/src/modules/ingest/replay/replay-source.service.ts
    - apps/gutcallfun-core/src/modules/ingest/replay/replay-starter.service.ts
    - apps/gutcallfun-core/scripts/replay-create.ts
  key_links:
    - "ReplayStarterService.start(gameId) injects EventIngestService (PipelineModule) and Repository<GameEvent>/<Game> — it auto-wipes then drives the ported replay emitter into processEvent."
    - "HistoricalClient reuses TxlineHttpClient (exported by FixturesModule, Plan 04) for the authenticated historical fetch."
    - "ReplayModule imports PipelineModule + FixturesModule + forFeature([GameEntity, GameEventEntity]); exports ReplayStarterService for the Plan-07 scheduler."
  prohibitions:
    - "auto-wipe must never delete a non-replay (is_replay=false) game's game_event rows — the guard is mandatory (append-only for live is sacred)."
    - "the replay path must never introduce a downstream mode flag — processEvent is source-agnostic (RPLY-02)."
    - "speed>1 must never be reachable from a user-facing/API surface — headless/script only (D-04)."
    - "the pre-match segment must never be compressed or skipped (D-04 — do not inherit the reference maxGapMs clamp)."
  flagged_assumptions:
    - "RPLY-01 concurrency edge resolved as covered (live+replay coexistence / per-game isolation is D-02's hard constraint, enforced by per-game mutex + separate connections)."
---

<objective>
Build the replay side of the source switch: port the reference `replay.ts` emitter (timestamp rebasing D-03, pacing D-04 with the pre-match clamp removed), the two recorded sources (A: historical fetch; B: captured NDJSON / own game_event rows — the same path boot-recovery reuses), a ReplayStarterService that auto-wipes then drives the emitter into the shared pipeline (D-07), and the `replay:create` dev script for demo choreography (D-06/D-08). A replay game drives the identical downstream pipeline as a live game — is_replay switches ONLY the source (RPLY-02).

Purpose: The demo is judged primarily and recorded from replay — the production pipeline must process real recorded match data (no mockups). This plan is the demo path.
Output: A callable ReplayStarterService.start(gameId) + `npm run replay:create` arming tool.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding allowlist for plan-review):
- `replay.ts` (ported): timestamp-rebasing emitter, pacing by original gaps ÷ speed, pre-match clamp removed (D-04 override of reference maxGapMs).
- `HistoricalClient` (historical.client.ts): `fetch(fixtureId)` over GET /api/scores/historical/{fixtureId} via TxlineHttpClient; in-memory per-process cache (Source A).
- `ReplaySourceService` (replay-source.service.ts): `load(game)` returning an ordered raw-event array from Source A (historical) or Source B (NDJSON file / own game_event rows), with A→B fallback.
- `ReplayStarterService` (replay-starter.service.ts): `start(gameId)` — is_replay-guarded auto-wipe + denormalized reset, then drives replay.ts into EventIngestService.processEvent.
- `replay-create.ts` (tsx script) + `replay:create` npm script: creates an is_replay=true game (starts_at=now()+~3min, supplied names).
- Fills `ReplayModule` (imports PipelineModule + FixturesModule + forFeature([GameEntity, GameEventEntity]); exports ReplayStarterService).
</artifacts_produced>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md
@.claude/skills/txodds-api/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port replay.ts (rebasing + pacing) + Source A/B loader (RPLY-01, D-01, D-03, D-04)</name>
  <files>apps/gutcallfun-core/src/modules/ingest/replay/replay.ts, apps/gutcallfun-core/src/modules/ingest/replay/historical.client.ts, apps/gutcallfun-core/src/modules/ingest/replay/replay-source.service.ts, apps/gutcallfun-core/src/modules/ingest/replay/replay.spec.ts</files>
  <read_first>
    - https://github.com/mckrava/txodds-txline-api-monitor — fetch the REAL `replay.ts` at implementation time (RESEARCH Open Question 1 / Assumption A1). NOTE the maxGapMs=5000 pre-match clamp MUST be removed/overridden for the pre-match segment (D-04 conflicts with port-as-is; CONTEXT wins — document the deviation). Scan for leaked tokens.
    - .claude/skills/txodds-api/references/sse-streaming.md (historical endpoint GET /api/scores/historical/{fixtureId}; replay pattern; retention window 2wk–6h)
    - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.ts (Plan 04 — reuse for authenticated historical fetch)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md (D-01 two-source, D-03 rebasing, D-04 pacing/pre-match, D-05 no VirtualFixture dependency)
  </read_first>
  <behavior>
    - Rebasing: every emitted event's Ts is shifted by delta = now() − firstEvent.Ts.
    - Pacing: inter-event delay = originalGap ÷ speed; speed=1 preserves original ratios; pre-match large gaps are NOT clamped.
    - Source A returns the historical array (cached); if empty, loader falls back to Source B (NDJSON / own game_event rows) as an ordered raw-event array.
  </behavior>
  <action>
    Fetch and port `replay.ts` near-verbatim as a pure/injectable emitter that takes an ordered raw-event array and an async per-event callback. Preserve timestamp rebasing (delta = now() − firstEvent.Ts added to every Ts — D-03) and pacing (original inter-event gap ÷ speed, default 1 — D-04). REMOVE/override the reference repo's `maxGapMs` (5000ms) pre-match clamp so the pre-match segment plays at full original pacing with no compression/skip (D-04); leave a comment marking this deliberate deviation from port-as-is. Implement `HistoricalClient.fetch(fixtureId)` over GET /api/scores/historical/{fixtureId} via TxlineHttpClient with an in-memory per-process cache (Source A). Implement `ReplaySourceService.load(game)` returning the ordered raw-event array: try Source A (historical) first; on empty (retention miss — RESEARCH Pitfall 2), fall back to Source B (read a captured NDJSON file path or the game's own game_event rows ORDER BY seq). Do NOT depend on TxLINE VirtualFixture (D-05). Write `replay.spec.ts` proving rebasing shifts Ts by the expected delta, pacing scales by speed, pre-match gaps are not clamped, and A→B fallback on empty Source A.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/replay/replay.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec proves emitted Ts values are rebased by delta = now() − firstEvent.Ts.
    - Spec proves inter-event delay scales as originalGap ÷ speed and a large pre-match gap is NOT clamped to 5000ms.
    - Spec proves ReplaySourceService falls back to Source B when Source A (historical) returns empty.
    - `grep -n "maxGapMs" apps/gutcallfun-core/src/modules/ingest/replay/replay.ts` shows the clamp removed or overridden with a deviation comment.
  </acceptance_criteria>
  <done>replay.ts ported with rebasing + uncompressed pre-match pacing; Source A/B loader with retention-miss fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReplayStarterService with is_replay-guarded auto-wipe (D-07, RPLY-02, RPLY-03)</name>
  <files>apps/gutcallfun-core/src/modules/ingest/replay/replay-starter.service.ts, apps/gutcallfun-core/src/modules/ingest/replay/replay.module.ts, apps/gutcallfun-core/src/modules/ingest/replay/replay-starter.service.spec.ts</files>
  <read_first>
    - apps/gutcallfun-core/src/modules/ingest/replay/replay.ts, replay-source.service.ts (Task 1)
    - apps/gutcallfun-core/src/modules/ingest/persistence/event-ingest.service.ts (Plan 03 — the shared processEvent; exported by PipelineModule)
    - apps/gutcallfun-core/src/models/game/game-event.entity.ts, game.entity.ts (auto-wipe target: delete game_event WHERE game_id; reset score_p1/p2/current_status_id/stream_cursor)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md (D-07 auto-wipe scoped to replay rows; D-02 downstream source-unaware)
  </read_first>
  <behavior>
    - start(gameId) for an is_replay=true game deletes that game's prior game_event rows and resets denormalized fields, THEN emits.
    - start(gameId) for an is_replay=false game refuses the auto-wipe (throws / no-op) — a live log is never wiped.
    - Emission calls EventIngestService.processEvent(gameId, raw) with no mode flag.
  </behavior>
  <action>
    Implement `ReplayStarterService.start(gameId)` (`@Injectable()`): load the game; assert is_replay=true (if false, refuse — never wipe a live log, D-07 guard); delete that game's prior game_event rows and reset denormalized score_p1/p2, current_status_id, stream_cursor/stream_cursor_at (so take #2 of the same row never collides with UNIQUE(game_id, seq)); load the ordered raw-event array via ReplaySourceService; drive the ported replay emitter, calling EventIngestService.processEvent(gameId, raw) for each rebased event (identical entry point as live — RPLY-02). Fill `ReplayModule`: import PipelineModule, FixturesModule, TypeOrmModule.forFeature([GameEntity, GameEventEntity]); provide HistoricalClient, ReplaySourceService, ReplayStarterService; export ReplayStarterService (Plan-07 scheduler invokes it). Write a spec (mocked repos/emitter/processEvent) proving: is_replay=true triggers wipe-then-emit into processEvent; is_replay=false refuses the wipe.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/replay/replay-starter.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec proves an is_replay=true game's game_event rows are deleted and denormalized fields reset before emission, then processEvent is called per event with no mode flag.
    - Spec proves an is_replay=false game is refused (no delete, throws or no-ops) — the auto-wipe guard holds.
    - `npm run build` compiles; ReplayModule exports ReplayStarterService.
  </acceptance_criteria>
  <done>Replay start auto-wipes only replay rows then drives the shared pipeline; live logs are never wiped.</done>
</task>

<task type="auto">
  <name>Task 3: replay:create dev arming script (D-06, D-08, RPLY-03)</name>
  <files>apps/gutcallfun-core/scripts/replay-create.ts, apps/gutcallfun-core/package.json</files>
  <read_first>
    - apps/gutcallfun-core/src/db/data-source.ts (standalone DataSource pattern for a tsx script — reuse this instance)
    - apps/gutcallfun-core/package.json (scripts block — add replay:create alongside migration:* using tsx, mirroring their invocation style)
    - apps/gutcallfun-core/src/models/game/game.entity.ts (game columns to set: fixtureId, isReplay, startsAt, team1/2Name, status)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md (D-06 dev/admin action; D-08 starts_at=now()+~3min choreography; RPLY-03 multiple takes = multiple replay rows)
  </read_first>
  <action>
    Create `scripts/replay-create.ts`, a standalone tsx script invoked as `npm run replay:create -- <fixtureId> [--speed N]`. It builds a DataSource from the shared `src/db/data-source.ts` and inserts a game row with is_replay=true, status='scheduled', starts_at=now()+~3min (D-08 choreography — renders as genuinely upcoming), fixtureId from the arg, and supplied team names (either from a fixtures lookup or script args; keep it simple — accept names as optional flags, default to placeholders derived from fixtureId). Because uq_game_fixture_live is partial (WHERE NOT is_replay), inserting multiple replay rows for the same fixture is permitted (RPLY-03 multiple takes). Persist the speed factor where the emitter/starter can read it (e.g. a games column is not available — pass via env or a small replay-config the starter reads; keep speed>1 script-only, never user-facing, D-04). Add the `replay:create` script to package.json scripts using the same tsx invocation style as the migration scripts. Validate fixtureId is a positive integer before use.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && node -e "const s=require('./package.json').scripts; if(!s['replay:create']) throw new Error('replay:create script missing'); console.log('replay:create present')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains a `replay:create` script invoked via tsx.
    - The script validates fixtureId as a positive integer and inserts an is_replay=true, status='scheduled', starts_at≈now()+3min game row (verifiable by reading the script logic; a manual run against docker Postgres inserts a browsable upcoming replay row).
    - Multiple invocations for the same fixtureId create multiple replay rows (no unique collision — partial index).
  </acceptance_criteria>
  <done>`npm run replay:create -- <fixtureId>` arms a replay game for demo choreography; speed>1 remains script-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ported replay.ts → repo | External code; scan for leaked tokens |
| replay auto-wipe → game_event | Destructive DELETE that must be scoped to replay rows only |
| dev script → DB | Admin/dev-only arming surface (not user-facing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Tampering / data loss | auto-wipe on replay start | high | mitigate | DELETE guarded by is_replay=true; refuse to wipe a live game's append-only log (D-07). Spec asserts the guard. |
| T-02-06-02 | Elevation of Privilege | replay:create / manual-DB-flip demo surface | medium | mitigate | Dev/admin-only (CLI script + direct DB UPDATE); no user-facing API creates replays; speed>1 script-only (D-04). |
| T-02-06-03 | Tampering | live vs replay isolation | high | mitigate | Replay drives its own per-game emitter into the shared per-game-mutex pipeline; a running replay never affects live ingest (D-02 hard constraint). |
| T-02-06-04 | Information Disclosure | ported replay.ts / historical fetch | high | mitigate | Never log credentials on historical fetch; scan ported file for hard-coded tokens. |
</threat_model>

<verification>
- replay.spec proves rebasing, uncompressed pre-match pacing, A→B fallback.
- replay-starter.spec proves is_replay-guarded auto-wipe + mode-flagless processEvent emission.
- package.json contains replay:create; script validates fixtureId and inserts a replay row (multiple takes allowed).
- `npm run build` compiles; ReplayModule exports ReplayStarterService.
- No schema DDL (is_replay/stream_cursor/game_event columns already exist).
</verification>

<success_criteria>
A replay game drives the identical downstream pipeline as live (source-only difference, no mode flag), with rebased timestamps, uncompressed pre-match pacing, Source A/B loading, is_replay-scoped auto-wipe, and a `replay:create` dev arming tool for demo choreography.
</success_criteria>

<output>
Create `.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-06-SUMMARY.md` when done.
</output>
