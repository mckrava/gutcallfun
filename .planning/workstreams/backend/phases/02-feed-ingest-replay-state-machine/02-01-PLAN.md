---
phase: 02-feed-ingest-replay-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gutcallfun-core/package.json
  - apps/gutcallfun-core/src/config/app-config.schema.ts
  - apps/gutcallfun-core/src/config/app-config.schema.spec.ts
  - apps/gutcallfun-core/src/app.module.ts
  - apps/gutcallfun-core/src/modules/ingest/ingest.module.ts
  - apps/gutcallfun-core/src/modules/ingest/shared/ingest-shared.module.ts
  - apps/gutcallfun-core/src/modules/ingest/pipeline/pipeline.module.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures.module.ts
  - apps/gutcallfun-core/src/modules/ingest/stream/stream.module.ts
  - apps/gutcallfun-core/src/modules/ingest/replay/replay.module.ts
  - apps/gutcallfun-core/src/modules/ingest/recovery/recovery.module.ts
  - apps/gutcallfun-core/src/modules/ingest/state/game-mutex.registry.ts
  - apps/gutcallfun-core/src/modules/ingest/state/game-state.registry.ts
  - apps/gutcallfun-core/src/modules/ingest/state/game-state.types.ts
  - apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.event.ts
  - apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.emitter.ts
  - apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.listener.ts
autonomous: true
requirements: [INGST-01, STAT-01, STAT-02]

assumption_delta_decision:
  primary_noun: "event source"
  decision: promote
  rationale: "Phase 2 introduces a second event SOURCE (live SSE vs replay emitter) behind one pipeline. Per D-02 the general event-source abstraction is primary and source-unaware downstream; live/replay are variants, not add-alongside branches. The shared registries + gap seam built here are consumed identically by both sources."

must_haves:
  truths:
    - "The app refuses to boot when TXLINE_GUEST_JWT or TXLINE_API_TOKEN is missing/blank (fail-fast validateSync throws), and boots cleanly when both are present plus SERVICE_LEVEL_ID=12 (INGST-01)."
    - "SERVICE_LEVEL_ID is asserted equal to 12 at boot; a value of 1 (60s-delayed feed) or any non-12 value crashes startup with a clear error (RESEARCH Pitfall 6)."
    - "GameMutexRegistry returns the SAME Mutex instance for repeated calls with the same gameId and a distinct Mutex for a different gameId (STAT-02 mechanism)."
    - "GameStateRegistry.getOrCreate returns a defined initial GameState (score 0-0, no attack run, null status) for a gameId with no prior state, and remove(gameId) evicts it so the map does not grow unbounded across finished games (STAT-01 store; RESEARCH Security Domain)."
    - "GameStreamGapEmitter emits a typed GameStreamGapDetected {gameId, expectedSeq, actualSeq, at} and the Phase-2 log-only listener records it without throwing (D-13 seam; contract frozen for Phase 4 to replace the listener body)."
    - "TxLINE credential env values are never passed to any Logger or console call anywhere in src/modules/ingest and src/config."
    - statement: "Re-running config validation twice on the same valid env produces identical validated output with no side effects (INGST-01 idempotency edge)."
      verification: backstop
  artifacts:
    - apps/gutcallfun-core/src/config/app-config.schema.ts
    - apps/gutcallfun-core/src/modules/ingest/ingest.module.ts
    - apps/gutcallfun-core/src/modules/ingest/shared/ingest-shared.module.ts
    - apps/gutcallfun-core/src/modules/ingest/state/game-mutex.registry.ts
    - apps/gutcallfun-core/src/modules/ingest/state/game-state.registry.ts
    - apps/gutcallfun-core/src/modules/ingest/state/game-state.types.ts
    - apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.event.ts
    - apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.emitter.ts
    - apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.listener.ts
  key_links:
    - "AppModule imports IngestModule after DatabaseModule (config-then-database-then-feature order)."
    - "IngestModule imports ScheduleModule.forRoot(), IngestSharedModule (@Global), and the five feature sub-modules (pipeline/fixtures/stream/replay/recovery stubs)."
    - "IngestSharedModule (@Global) provides+exports GameMutexRegistry, GameStateRegistry, GameStreamGapEmitter so every ingest sub-module can inject them without re-importing."
  prohibitions:
    - "TxLINE JWT or API token values must never be written to a log, console, error message, or committed file."
    - "SERVICE_LEVEL_ID must never silently default to a non-12 value (would ship a 60s-delayed feed under a live premise)."
  flagged_assumptions:
    - "STAT-02 (concurrency, unclassified in the edge probe): planner assumption — the ONLY concurrency guarantee this phase provides is per-game serial processing via one async-mutex Mutex per gameId; there is NO cross-game ordering guarantee and NO distributed lock (single-process deployment, D-02). Surfaced for reviewer confirmation."
---

<objective>
Establish the ingest subsystem scaffold every other Phase-2 plan builds on: extend the fail-fast config schema with TxLINE credentials and the SL=12 assertion, install the two locked new dependencies, create IngestModule with five empty feature sub-modules (so later plans fill their own module file with zero cross-plan file conflict), and provide the three shared source-agnostic primitives — the per-game mutex registry (STAT-02), the per-game state registry (STAT-01 store), and the GameStreamGapDetected typed event seam with a log-only listener (D-13).

Purpose: Lock the DI contract and the source-unaware seams once, so the pipeline, live-source, replay-source, and recovery plans plug in without re-negotiating module wiring or the gap-event contract. This is the "promote event source to primary" foundation (assumption_delta_decision above).
Output: A bootable app with an IngestModule scaffold, validated new config, and three injectable singletons available @Global across ingest.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding allowlist for plan-review):
- Config fields on `EnvironmentVariables`: `TXLINE_GUEST_JWT`, `TXLINE_API_TOKEN`, `PAST_FIXTURES_COUNT` (default 20), `SERVICE_LEVEL_ID` (asserted 12).
- NestJS modules: `IngestModule`, `IngestSharedModule` (@Global), `PipelineModule` (stub), `FixturesModule` (stub), `StreamModule` (stub), `ReplayModule` (stub), `RecoveryModule` (stub).
- Providers: `GameMutexRegistry`, `GameStateRegistry`, `GameStreamGapEmitter`, `GameStreamGapLogListener`.
- Types: `GameState` (interface), `PossessionStage` (union), `GameStreamGapDetected` (typed event payload).
- npm deps added: `@nestjs/schedule@6.1.3`, `async-mutex@0.5.0`.
</artifacts_produced>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install locked deps + extend fail-fast config schema (INGST-01, SL=12 assertion)</name>
  <files>apps/gutcallfun-core/package.json, apps/gutcallfun-core/src/config/app-config.schema.ts, apps/gutcallfun-core/src/config/app-config.schema.spec.ts</files>
  <read_first>
    - apps/gutcallfun-core/src/config/app-config.schema.ts (the file being modified — extend the existing EnvironmentVariables class in place, do NOT rewrite)
    - apps/gutcallfun-core/src/config/app-config.schema.spec.ts (extend existing tests)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md (section "src/config/app-config.schema.ts" — the exact @Is* decorator convention to mirror)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Open Question 2 + Pitfall 6 — new fields and the SL=12 startup assertion)
  </read_first>
  <action>
    Install the two locked new dependencies into the workspace: `@nestjs/schedule@6.1.3` and `async-mutex@0.5.0` (both Approved in RESEARCH Package Legitimacy Audit — no legitimacy checkpoint required). Do not change any existing pinned versions. Then extend the existing `EnvironmentVariables` class with four new fields following the exact same class-validator decorator style already used for DATABASE_URL/PORT/NODE_ENV: `TXLINE_GUEST_JWT: string` (`@IsString()`, required — no `@IsOptional`, no default), `TXLINE_API_TOKEN: string` (`@IsString()`, required), `PAST_FIXTURES_COUNT: number = 20` (`@IsOptional() @IsInt() @Min(1)`), and `SERVICE_LEVEL_ID: number` (`@IsInt()`, required). Enforce the SL=12 assertion (RESEARCH Pitfall 6): after the existing `validateSync` check in the `validate()` function, throw a clear Error if `validated.SERVICE_LEVEL_ID !== 12` (a value of 1 is the silent 60s-delayed feed). Keep the fail-fast philosophy — throw synchronously, never fall back to a default for the two credentials or for the service-level assertion. Add NO Logger/console statement referencing the credential values (the existing file logs nothing; preserve that).
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/config/app-config.schema.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @nestjs/schedule async-mutex` resolves to 6.1.3 and 0.5.0 respectively.
    - A spec case with valid env including SERVICE_LEVEL_ID=12 returns a validated instance; a case missing TXLINE_GUEST_JWT throws; a case with SERVICE_LEVEL_ID=1 throws with a message mentioning the service level.
    - `grep -rniE "guest_jwt|api_token" apps/gutcallfun-core/src/config` shows no `Logger`/`console` call receiving the value (assignment/validation only).
  </acceptance_criteria>
  <done>Config boots only with both TxLINE credentials present and SERVICE_LEVEL_ID=12; both new packages installed at the locked versions; no credential logging.</done>
</task>

<task type="auto">
  <name>Task 2: IngestModule scaffold with five feature sub-module stubs + app wiring</name>
  <files>apps/gutcallfun-core/src/app.module.ts, apps/gutcallfun-core/src/modules/ingest/ingest.module.ts, apps/gutcallfun-core/src/modules/ingest/shared/ingest-shared.module.ts, apps/gutcallfun-core/src/modules/ingest/pipeline/pipeline.module.ts, apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures.module.ts, apps/gutcallfun-core/src/modules/ingest/stream/stream.module.ts, apps/gutcallfun-core/src/modules/ingest/replay/replay.module.ts, apps/gutcallfun-core/src/modules/ingest/recovery/recovery.module.ts</files>
  <read_first>
    - apps/gutcallfun-core/src/app.module.ts (the file being modified — append IngestModule to imports)
    - apps/gutcallfun-core/src/modules/core/database.module.ts (module shape analog: TypeOrmModule usage)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md (section "ingest.module.ts" — app wiring order and forFeature idiom)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Recommended Project Structure — the modules/ingest tree)
  </read_first>
  <action>
    Create `IngestModule` importing `ScheduleModule.forRoot()` (from `@nestjs/schedule`, the locked cron choice), `IngestSharedModule`, and the five feature sub-modules `PipelineModule`, `FixturesModule`, `StreamModule`, `ReplayModule`, `RecoveryModule`. Create each of the five feature sub-modules as an EMPTY stub `@Module({})` (they exist so IngestModule compiles now; the pipeline/fixtures/stream/replay/recovery plans each fill exactly one of them later with zero shared-file conflict). Create `IngestSharedModule` marked `@Global()` — it will provide+export the three shared singletons in Task 3 (leave its providers/exports arrays ready for Task 3). Register `IngestModule` in `AppModule` by appending it to the existing `imports` array AFTER `DatabaseModule`, preserving the established config-then-database-then-feature order. Do not add controllers or a global forFeature at the IngestModule level — each sub-module declares its own `TypeOrmModule.forFeature` for the repositories it needs.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest test/app.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` compiles with all six new module files present.
    - The existing app e2e spec (`test/app.e2e-spec.ts`) still bootstraps the Nest application context without a DI resolution error, proving IngestModule + sub-modules load.
    - `grep -n "IngestModule" apps/gutcallfun-core/src/app.module.ts` shows it appended after DatabaseModule in imports.
  </acceptance_criteria>
  <done>App boots with IngestModule and five empty feature sub-modules wired; ScheduleModule.forRoot() active; IngestSharedModule declared @Global.</done>
</task>

<task type="auto">
  <name>Task 3: Shared source-agnostic primitives — mutex registry (STAT-02), state registry (STAT-01), gap seam (D-13)</name>
  <files>apps/gutcallfun-core/src/modules/ingest/state/game-mutex.registry.ts, apps/gutcallfun-core/src/modules/ingest/state/game-state.registry.ts, apps/gutcallfun-core/src/modules/ingest/state/game-state.types.ts, apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.event.ts, apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.emitter.ts, apps/gutcallfun-core/src/modules/ingest/events/game-stream-gap.listener.ts, apps/gutcallfun-core/src/modules/ingest/shared/ingest-shared.module.ts, apps/gutcallfun-core/src/modules/ingest/state/game-mutex.registry.spec.ts</files>
  <read_first>
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Pattern 2 GameMutexRegistry, Supporting stack: native EventEmitter wrapper, Security Domain: bound the per-game map)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md (sections "game-state.registry.ts" and "No Analog Found: game-stream-gap.emitter.ts")
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md (D-13 — the GameStreamGapDetected contract and the Phase-2 log-only listener)
  </read_first>
  <action>
    Define the `GameState` interface and `PossessionStage` union in `game-state.types.ts` (fields: gameId, score1, score2, currentStatusId, possessionStage, attackRun/high-water bookkeeping placeholders, lastFeedTs, lastSeq, connectionId) — the shared shape the state machine (Plan 03) will mutate; keep it a plain interface with an exported `createInitialGameState(gameId)` factory returning score 0-0, no attack run, null status. Implement `GameMutexRegistry` as an `@Injectable()` holding `Map<number, Mutex>` (from `async-mutex`) with `runExclusive<T>(gameId, fn)` and lazy per-game Mutex creation (RESEARCH Pattern 2), plus a `remove(gameId)` eviction method. Implement `GameStateRegistry` as an `@Injectable()` holding `Map<number, GameState>` with `getOrCreate(gameId)`, `get(gameId)`, and `remove(gameId)` (eviction for finished/cancelled games — RESEARCH Security Domain: unbounded map growth). Define the typed event payload `GameStreamGapDetected {gameId: number; expectedSeq: number; actualSeq: number; at: Date}` in `game-stream-gap.event.ts`. Implement `GameStreamGapEmitter` as an `@Injectable()` singleton wrapping a native Node `EventEmitter` (~15 lines, RESEARCH Standard Stack "Supporting") exposing typed `emit(payload)` and `on(handler)` — no new pub/sub dependency. Implement `GameStreamGapLogListener` as an `@Injectable()` that subscribes to the emitter on init (implement `OnModuleInit`) and logs each gap via `Logger.warn` (Phase-4 will replace this listener body with void+refund + goal adjudication — leave a comment marking the seam). Register all four providers in `IngestSharedModule` and export the three injectables (`GameMutexRegistry`, `GameStateRegistry`, `GameStreamGapEmitter`); the log listener is a provider only. Add a spec proving mutex identity-per-game and registry eviction.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/state/game-mutex.registry.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec asserts `registry.forGame(1)` twice returns the same Mutex reference and `forGame(2)` returns a different one (STAT-02 mechanism), and that GameStateRegistry.getOrCreate(1)+remove(1)+get(1) returns undefined after removal (eviction).
    - `game-stream-gap.event.ts` exports the `GameStreamGapDetected` type with exactly the four fields; emitting it drives the log listener (assert via a spy or a small emit-then-observe test).
    - `npm run build` compiles; IngestSharedModule exports the three injectables.
  </acceptance_criteria>
  <done>Three shared @Global singletons available across ingest; GameStreamGapDetected contract frozen with a log-only listener; per-game maps evictable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → app config | TxLINE JWT/API-token secrets cross from git-ignored env into process memory; must never re-cross into logs/commits |
| npm registry → build | New dependencies (`@nestjs/schedule`, `async-mutex`) enter the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | app-config.schema.ts / any ingest logger | high | mitigate | Never log/print TXLINE_GUEST_JWT / TXLINE_API_TOKEN; validation assigns only. Acceptance grep asserts no logger receives the value. |
| T-02-01-02 | Denial of Service | SERVICE_LEVEL_ID misconfig | medium | mitigate | Assert SL=12 at boot (fail-fast); SL=1 would silently ship a 60s-delayed feed under a live premise. |
| T-02-01-SC | Tampering | npm installs (`@nestjs/schedule`, `async-mutex`) | high | accept | Both packages Approved in RESEARCH Package Legitimacy Audit (registry+repo+downloads clean, no postinstall scripts); no [ASSUMED]/[SUS]/[SLOP] → no blocking human checkpoint required. Pins are exact. |
</threat_model>

<verification>
- `npm run build` compiles the whole app with the new module tree.
- Config specs prove fail-fast on missing credentials and on SERVICE_LEVEL_ID != 12.
- App e2e still bootstraps (IngestModule + sub-modules resolve).
- Mutex/state-registry spec proves per-game identity + eviction; gap emitter drives the log listener.
- No schema DDL introduced (confirmed: game.is_replay/stream_cursor and game_event promoted columns already exist from Phase 1 — no migration task needed this phase).
</verification>

<success_criteria>
The ingest scaffold boots, new config is fail-fast, and the three shared source-agnostic primitives (mutex registry, state registry, gap seam) are injectable @Global with proven per-game identity, eviction, and a working log-only D-13 listener.
</success_criteria>

<output>
Create `.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-01-SUMMARY.md` when done.
</output>
