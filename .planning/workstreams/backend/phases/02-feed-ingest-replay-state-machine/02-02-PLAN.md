---
phase: 02-feed-ingest-replay-state-machine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gutcallfun-core/src/modules/ingest/state/possession.ts
  - apps/gutcallfun-core/src/modules/ingest/state/goals.ts
  - apps/gutcallfun-core/src/modules/ingest/state/clock.ts
  - apps/gutcallfun-core/src/modules/ingest/state/possession.spec.ts
  - apps/gutcallfun-core/src/modules/ingest/state/goals.spec.ts
autonomous: true
requirements: [STAT-01, STAT-03]

must_haves:
  truths:
    - "Possession stage is classified by EXACT Action match against the four staged possession actions (safe_possession, attack_possession, danger_possession, high_danger_possession); a bare Action:'possession' message is treated as a ball-holder-change marker only and never as a danger stage (RESEARCH Anti-Pattern; txodds-api gotcha 4)."
    - "goals.ts dedups/reconciles on Id anchored to the earliest message for that Id: a goal's Confirmed:false followed later by Confirmed:true for the same Id counts as ONE goal, and an action_discarded for that Id removes it (RESEARCH Pitfall 3; txodds-api gotcha 3)."
    - "Replaying a captured/sample event sequence through possession.ts + goals.ts yields a final score equal to the sequence's terminal Score field, and no double-counted goal from confirm/amend/discard churn (STAT-01)."
    - "Match-time logic (attack-run segmentation, debounce windows) compares feed Ts deltas only; the ported code exposes feed-Ts helpers separate from any wall-clock use (STAT-03 two-clock discipline)."
    - "Possession classification for the empty/first event (no prior stage) returns a defined initial stage, not undefined/crash (STAT-01 empty edge)."
    - "Score integers never drift to float; score fields stay integer-valued across a full replay (STAT-01 precision edge)."
    - statement: "State self-heals from any single message after a gap — feeding an out-of-order/post-gap message reconstructs a consistent stage without requiring the missed messages (STAT-01 boundary edge)."
      verification: backstop
  artifacts:
    - apps/gutcallfun-core/src/modules/ingest/state/possession.ts
    - apps/gutcallfun-core/src/modules/ingest/state/goals.ts
    - apps/gutcallfun-core/src/modules/ingest/state/clock.ts
  key_links:
    - "possession.ts and goals.ts are PURE modules (no NestJS DI, no DB) importing the PascalCase feed message shape; Plan 03's game-state.machine.ts calls them."
    - "clock.ts holds the two-clock helpers (feed-Ts delta vs wall-clock) so STAT-03 has one canonical place; state machine (Plan 03) and Phase-4 windows both consume it."
  prohibitions:
    - "possession classification must never substring-match 'possession' — exact Action equality only."
    - "a single logical goal (shared Id across Confirmed:false/true/amend) must never be counted more than once in the score."
    - "match-time and user-time comparisons must never be mixed in one expression (two-clock discipline)."
  flagged_assumptions:
    - "STAT-03 (unclassified in the edge probe): planner assumption — Phase 2 only ESTABLISHES the two-clock split inside the state machine (feed Ts for match logic); the user-time authority (expires_at / answer lock on wall clock) is Phase 4's WNDW/ANSW surface. This plan proves the feed-Ts side and exposes the wall-clock helper unused for now. Surfaced for reviewer confirmation."
---

<objective>
Port the two battle-tested pure state-machine modules — `possession.ts` (danger-ladder / attack-run classification) and `goals.ts` (goal confirm/discard/amend reconciliation) — from the reference monitor repo near-verbatim, plus a small `clock.ts` that makes the two-clock discipline (STAT-03) explicit. These are pure functions with defined input/output, ideal for unit tests written against captured sample sequences.

Purpose: The feed-parsing edge cases (goal confirm/discard timing ~76s apart, Id-reuse across incident types, possession-ladder classification) are already solved against a 23,510-record corpus (CLAUDE.md LOCK). Re-solving them worse is the top risk — port, don't rebuild.
Output: Three pure modules the persistence pipeline (Plan 03) wires into a per-game state machine, with unit tests proving score correctness under confirm/discard churn.
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding allowlist for plan-review):
- `possession.ts`: exported possession-stage classifier + `PossessionStage`-producing function(s), attack-run/high-water bookkeeping helpers (ported names preserved where reasonable).
- `goals.ts`: exported Id-keyed goal reconciler (anchor-to-earliest), confirm/discard/amend handlers, score-derivation function.
- `clock.ts`: `feedElapsedMs(fromTs, toTs)` (match-time) and `wallClockNow()` / `isExpired(expiresAt)` (user-time) helpers making STAT-03 explicit.
</artifacts_produced>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md
@.claude/skills/txodds-api/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port possession.ts (danger ladder + attack-run classification) with unit tests</name>
  <files>apps/gutcallfun-core/src/modules/ingest/state/possession.ts, apps/gutcallfun-core/src/modules/ingest/state/clock.ts, apps/gutcallfun-core/src/modules/ingest/state/possession.spec.ts</files>
  <read_first>
    - https://github.com/mckrava/txodds-txline-api-monitor — fetch the REAL `possession.ts` at implementation time (WebFetch raw file, or `gh api` the repo contents). RESEARCH Open Question 1 + Assumption A1: the research summary is lossy, port the actual source, not the summary.
    - .claude/skills/txodds-api/references/message-reference.md (possession ladder actions + PossessionType enum; read via the txodds-api skill)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Anti-Patterns: exact-Action classification; Pattern 3 two-clock)
  </read_first>
  <behavior>
    - Given an attack_possession message, classifier returns the attack stage; given high_danger_possession, the high-danger stage; given a bare 'possession' message, it returns unchanged prior stage (marker, not a ladder step).
    - Given no prior state, the classifier returns a defined initial stage (not undefined).
    - Attack-run segmentation uses feed Ts deltas via clock.ts helpers, never Date.now().
  </behavior>
  <action>
    Fetch and port the real `possession.ts` near-verbatim as a PURE module (no NestJS decorators, no DB). Preserve the exact-Action classification against the four staged possession actions and the attack-run/high-water bookkeeping. Extract the feed-Ts delta arithmetic into `clock.ts` (`feedElapsedMs(fromTs,toTs)` for match-time; plus `wallClockNow()`/`isExpired(expiresAt)` stubs for the user-time side so STAT-03 has a canonical home) and import it from possession.ts — do not scatter raw `Date.now()` calls. Keep field access PascalCase (Action, Ts, PossessionType, Participant). Write `possession.spec.ts` covering: exact-match classification of all four ladder stages, the bare-'possession' marker case, the empty/first-event case, and an attack-run segmentation case driven by feed Ts deltas.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/state/possession.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec proves `attack_possession`/`danger_possession`/`high_danger_possession`/`safe_possession` classify to distinct stages and a bare `possession` message does not advance the ladder.
    - Spec proves first-event classification returns a defined stage (no crash/undefined).
    - `grep -n "Date.now" apps/gutcallfun-core/src/modules/ingest/state/possession.ts` returns nothing (match-time uses clock.ts feed-Ts helpers — STAT-03).
  </acceptance_criteria>
  <done>possession.ts ported pure, exact-Action classification proven, two-clock feed-Ts path routed through clock.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port goals.ts (Id-keyed confirm/discard/amend reconciliation) with unit tests</name>
  <files>apps/gutcallfun-core/src/modules/ingest/state/goals.ts, apps/gutcallfun-core/src/modules/ingest/state/goals.spec.ts</files>
  <read_first>
    - https://github.com/mckrava/txodds-txline-api-monitor — fetch the REAL `goals.ts` at implementation time (RESEARCH Open Question 1). Port the actual source.
    - .claude/skills/txodds-api/references/message-reference.md (goal / action_amend / action_discarded semantics; Confirmed field; Id dedup)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Pitfall 3 — corrections are not new events; anchor-to-earliest)
    - apps/gutcallfun-core/src/modules/ingest/state/clock.ts (created in Task 1 — reuse feed-Ts helpers)
  </read_first>
  <behavior>
    - A goal with Confirmed:false then later Confirmed:true sharing one Id → score increments by exactly 1 (one logical goal).
    - An action_discarded for a previously-counted goal Id → score decrements back.
    - Replaying a sample sequence → final derived score equals the sequence's terminal Score field.
  </behavior>
  <action>
    Fetch and port the real `goals.ts` near-verbatim as a PURE module. Preserve the Id-keyed dedup/reconciliation anchored to the earliest message per Id (a goal's Confirmed:false can precede Confirmed:true by ~76s — same Id, one goal). Handle action_amend (retroactive modify) and action_discarded (retroactive remove) by Id. Expose a score-derivation function the state machine calls after each event. Write `goals.spec.ts` with: the confirm-after-unconfirmed single-count case, the discard-a-counted-goal case, and a multi-goal sample sequence asserting final derived score == terminal Score field. Keep score values integer.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/state/goals.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec proves Confirmed:false→true on one Id yields a single score increment (no double count).
    - Spec proves action_discarded on a counted goal Id reverses the increment.
    - Spec proves a replayed sample sequence's derived score equals its terminal Score field, and all score values are integers.
  </acceptance_criteria>
  <done>goals.ts ported pure with Id-anchored reconciliation; confirm/discard churn never double-counts; score integer-exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ported external source → repo | Code fetched from the monitor repo enters this codebase; scan for leaked tokens (CLAUDE.md security rule) |
| untrusted feed message → state machine | PascalCase feed JSON is external input; classification must not crash on unexpected field shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | ported possession.ts / goals.ts | medium | mitigate | Scan the ported source for any hard-coded credential/token before commit (CLAUDE.md: scan ported code for leaked tokens); these modules are pure logic, no secrets belong here. |
| T-02-02-02 | Denial of Service | classifier on malformed feed field | medium | mitigate | Defensive field access (optional-chaining / defaults) so an unexpected/missing PossessionType or Confirmed does not throw; the persist-and-ignore contract (Plan 03) backstops at the pipeline. |
</threat_model>

<verification>
- Unit specs for possession.ts and goals.ts pass (score correctness, exact-Action classification, Id reconciliation).
- `npm run build` compiles the pure modules.
- No hard-coded token strings in the ported files (grep scan).
</verification>

<success_criteria>
possession.ts and goals.ts are ported near-verbatim as pure modules with unit tests proving exact-Action possession classification and Id-anchored goal reconciliation (no double-count under confirm/discard churn); clock.ts makes STAT-03 explicit.
</success_criteria>

<output>
Create `.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-02-SUMMARY.md` when done.
</output>
