---
phase: 02-feed-ingest-replay-state-machine
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-fixtures.client.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures.module.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-fixtures.client.spec.ts
  - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.spec.ts
autonomous: true
requirements: [GAME-01, INGST-01]

must_haves:
  truths:
    - "The TxLINE HTTP client attaches Authorization: Bearer <TXLINE_GUEST_JWT> and X-Api-Token: <TXLINE_API_TOKEN> to every request, reading both from validated config (INGST-01)."
    - "On a 401 the client refreshes the guest JWT from the same host ONCE and retries the request once; it never loop-retries a dead JWT (INGST-01; RESEARCH Security: 401 retry-storm mitigation)."
    - "fixtureId (and any id interpolated into a TxLINE URL) is validated as a positive integer before interpolation (RESEARCH Security: query-string injection via fixtureId)."
    - "The fixtures cron runs every minute (@Cron EVERY_MINUTE) and discovers fixtures with team names, competition, fixture_group_id, and starts_at from the fixtures endpoint (not the numeric-only stream) (GAME-01)."
    - "Past fixtures are pulled by calling /api/fixtures/snapshot with startEpochDay set PAST_FIXTURES_COUNT days in the PAST (endpoint is forward-only) and filtering client-side to the last N by StartTime desc, marked status='finished' (D-09; RESEARCH Pitfall 1)."
    - "Upcoming fixtures use a 7-day-ahead window; discovery is upsert-only by fixture_id (create/update); vanished fixtures are left as-is (no delete/reconcile); no competition filter — every soccer fixture returned is taken (D-10, D-11, D-12)."
    - "An empty fixtures response completes the cron with zero rows created and no crash (GAME-01 empty edge)."
    - "Overlapping 1-minute cron ticks do not double-insert — upsert keyed on integer fixture_id equality is idempotent (GAME-01 concurrency/encoding edges)."
    - statement: "starts_at is stored as timestamptz from the fixture StartTime without timezone drift or truncation (GAME-01 precision edge)."
      verification: backstop
  artifacts:
    - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.ts
    - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-fixtures.client.ts
    - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts
  key_links:
    - "TxlineHttpClient is the shared authenticated fetch wrapper reused by fixtures (this plan), the SSE client (Plan 05), and the historical client (Plan 06) — INGST-01 lives here once."
    - "FixturesCronService injects Repository<GameEntity> (TypeOrmModule.forFeature) and upserts by fixture_id."
    - "FixturesModule imports IngestSharedModule (@Global registries available) and TypeOrmModule.forFeature([GameEntity]); exports TxlineHttpClient for Plans 05/06."
  prohibitions:
    - "TxLINE JWT/API-token values must never be logged, printed, or committed."
    - "a 401 must never trigger a retry loop — refresh once, retry once, then surface the error."
    - "fixtures discovery must never delete or hard-reconcile vanished fixtures (upsert-only per D-11)."
  flagged_assumptions:
    - "INGST-01 idempotency/concurrency edges resolved as covered above; no unclassified edges assigned to this plan."
---

<objective>
Deliver fixtures discovery (GAME-01) and the shared TxLINE authentication client (INGST-01) that every TxLINE call in this phase reuses. The cron pulls both the forward window (7 days upcoming) and — because the feed currently has no live/upcoming fixtures and the snapshot endpoint is forward-only — the last N PAST fixtures as browsable replay source material (D-09), upserting real team names/metadata into game rows.

Purpose: Populate the game table with real, richly-named fixtures so the demo has browsable match cards, and centralize the guest-JWT + X-Api-Token + 401-refresh auth logic in one client the SSE and replay plans reuse.
Output: TxlineHttpClient (INGST-01), fixtures snapshot client, and a 1-minute discovery cron (GAME-01).
</objective>

<artifacts_produced>
New symbols this plan creates (source-grounding allowlist for plan-review):
- `TxlineHttpClient` (txline-http.client.ts): authenticated `request()` wrapper — Bearer JWT + X-Api-Token headers, 401→refresh-once-retry-once, positive-integer fixtureId guard, JWT refresh via POST /auth/guest/start.
- `TxlineFixturesClient` (txline-fixtures.client.ts): `snapshot(startEpochDay)` over GET /api/fixtures/snapshot; `fetchPast(count)` + `fetchUpcoming(days)` helpers.
- `FixturesCronService` (fixtures-cron.service.ts): `@Cron(EVERY_MINUTE) discoverFixtures()` — upsert by fixture_id.
- Fills `FixturesModule` (imports forFeature([GameEntity]) + IngestSharedModule; providers TxlineHttpClient/TxlineFixturesClient/FixturesCronService; exports TxlineHttpClient).
</artifacts_produced>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-PATTERNS.md
@.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md
@apps/gutcallfun-core/src/models/game/game.entity.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared TxLINE HTTP/auth client — Bearer + X-Api-Token, 401 refresh-once (INGST-01)</name>
  <files>apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.ts, apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.spec.ts</files>
  <read_first>
    - .claude/skills/txodds-api/SKILL.md and .claude/skills/txodds-api/references/auth-and-subscription.md + sse-streaming.md (auth headers, guest-JWT refresh endpoint POST /auth/guest/start, 401 CRITICAL gotcha: re-auth once, never loop; apiToken wallet-durable / JWT rotates)
    - apps/gutcallfun-core/src/config/app-config.schema.ts (Plan 01 — TXLINE_GUEST_JWT, TXLINE_API_TOKEN config fields)
    - apps/gutcallfun-core/src/modules/core/database.module.ts (ConfigService constructor-injection convention)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Security Domain: 401 retry storm; query-string injection via fixtureId)
  </read_first>
  <behavior>
    - request() attaches Authorization: Bearer <jwt> and X-Api-Token: <token> on every call.
    - A 200 response returns the parsed body; a 401 triggers exactly one JWT refresh + one retry, then succeeds or surfaces the error.
    - A second consecutive 401 (dead after refresh) is NOT retried again — no loop.
    - A non-integer / negative fixtureId passed to a URL builder throws before any network call.
  </behavior>
  <action>
    Implement `TxlineHttpClient` as an `@Injectable()` reading TXLINE_GUEST_JWT / TXLINE_API_TOKEN via ConfigService. Provide a `request(path, init?)` using native fetch that always sets the two auth headers against the TxLINE origin (https://txline.txodds.com). On a 401, call a private `refreshGuestJwt()` (POST /auth/guest/start, capture the new token in memory) and retry the original request ONCE; if it 401s again, throw an AUTH_EXPIRED-style error — never loop (RESEARCH Security / txodds-api gotcha). Provide a `buildFixtureUrl(base, fixtureId)`-style guard that validates fixtureId is a positive integer before interpolation (RESEARCH Security: tampering). Log only presence/expiry-countdown of credentials, never the values. Write a spec with a mocked fetch proving: headers attached; one-refresh-one-retry on 401; no second retry on a repeated 401; integer-guard throws on a bad fixtureId.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/fixtures/txline-http.client.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec: both auth headers present on requests; single 401→refresh→retry; repeated 401 surfaces an error without a third attempt.
    - Spec: fixtureId guard rejects 0/negative/non-integer before fetch.
    - `grep -rniE "guest_jwt|api_token|Bearer " apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.ts` shows no logger/console emitting the token value.
  </acceptance_criteria>
  <done>One authenticated TxLINE client with safe 401 refresh, no retry storm, integer-guarded URLs, no credential logging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixtures snapshot client + 1-minute discovery cron with past-fixture pull (GAME-01)</name>
  <files>apps/gutcallfun-core/src/modules/ingest/fixtures/txline-fixtures.client.ts, apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts, apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures.module.ts, apps/gutcallfun-core/src/modules/ingest/fixtures/txline-fixtures.client.spec.ts, apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.spec.ts</files>
  <read_first>
    - .claude/skills/txodds-api/references/txline-endpoints.md + txline-openapi.yaml (GET /api/fixtures/snapshot contract: startEpochDay, forward-only 30-day window; Fixture schema fields StartTime, team names, competition, fixture_group_id)
    - apps/gutcallfun-core/src/modules/ingest/fixtures/txline-http.client.ts (Task 1 — reuse for authenticated calls)
    - apps/gutcallfun-core/src/models/game/game.entity.ts (upsert target columns: fixtureId, team1/2Name, competition, fixtureGroupId, startsAt, status, participant1/2Id)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-RESEARCH.md (Pattern 5 @Cron; Pitfall 1 forward-only snapshot)
    - .planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-CONTEXT.md (D-09 past pull, D-10 no filter, D-11 7d upcoming + upsert-only, D-12 all live)
  </read_first>
  <behavior>
    - fetchUpcoming(7) calls snapshot(today) and returns fixtures within 7 days.
    - fetchPast(N) calls snapshot(today − N days) and returns the last N fixtures by StartTime desc, marked finished.
    - discoverFixtures upserts by fixture_id: an existing fixture updates starts_at/names; a new one inserts; a fixture no longer returned is left untouched.
    - An empty snapshot response results in zero upserts, no throw.
  </behavior>
  <action>
    Implement `TxlineFixturesClient` with `snapshot(startEpochDay)` over GET /api/fixtures/snapshot (via TxlineHttpClient), plus `fetchUpcoming(days=7)` (startEpochDay=today) and `fetchPast(count=PAST_FIXTURES_COUNT)` — the latter MUST set startEpochDay to `today − count` days so the forward-only 30-day window includes already-started fixtures (RESEARCH Pitfall 1 / D-09), then filter/sort client-side by StartTime desc and take the last N. Implement `FixturesCronService` with `@Cron(CronExpression.EVERY_MINUTE) discoverFixtures()` that pulls both windows, maps each Fixture to game columns (team1/2Name, competition, fixture_group_id, starts_at, participant ids), applies NO competition filter (D-10) and NO live cap (D-12), marks past fixtures status='finished' and upcoming as scheduled, and upserts by fixture_id (create/update; vanished fixtures left as-is per D-11). Fill `FixturesModule`: import TypeOrmModule.forFeature([GameEntity]) + IngestSharedModule; provide TxlineHttpClient, TxlineFixturesClient, FixturesCronService; export TxlineHttpClient (Plans 05/06 reuse it). Write specs (mocked http client + mocked repository) for: fetchPast uses a past startEpochDay and returns last-N desc; discover upserts by fixture_id (update vs insert); empty response no-crash.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/ingest/fixtures/txline-fixtures.client.spec.ts src/modules/ingest/fixtures/fixtures-cron.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec proves fetchPast(N) requests snapshot with startEpochDay in the past and returns the last N by StartTime desc marked finished.
    - Spec proves discoverFixtures upserts by fixture_id (an existing fixture_id updates, a new one inserts) and applies no competition filter.
    - Spec proves an empty snapshot response performs zero upserts without throwing.
    - `npm run build` compiles; FixturesModule exports TxlineHttpClient.
  </acceptance_criteria>
  <done>Fixtures cron discovers past + upcoming fixtures with real names/metadata every minute, upsert-only by fixture_id, no filter/cap; forward-only endpoint handled via past startEpochDay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → TxLINE API | Outbound authenticated calls carrying secrets; auth failures must not storm |
| TxLINE response → game rows | Untrusted fixture JSON mapped into DB; ids interpolated into URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | TxlineHttpClient credential handling | high | mitigate | Never log JWT/token values; log only presence/expiry-countdown. |
| T-02-04-02 | Denial of Service | 401 retry storm against TxLINE | high | mitigate | Refresh once + retry once; never loop a dead JWT (rate-limit self-protection). |
| T-02-04-03 | Tampering | fixtureId interpolated into TxLINE URLs | medium | mitigate | Positive-integer guard before interpolation. |
| T-02-04-04 | Tampering | fixture JSON → game upsert | low | accept | Upsert-only by fixture_id; malformed fields land as nullable columns; no destructive reconcile (D-11). |
</threat_model>

<verification>
- HTTP client spec proves headers, safe 401 refresh, integer guard, no credential logging.
- Fixtures client/cron specs prove past startEpochDay pull, upsert-by-fixture_id, empty-response safety, no filter/cap.
- `npm run build` compiles; FixturesModule exports TxlineHttpClient.
- No schema DDL (game columns already exist).
</verification>

<success_criteria>
The 1-minute cron discovers past + upcoming fixtures with real team names/competition/starts_at, upsert-only by fixture_id with no filter or cap, using a past startEpochDay to defeat the forward-only endpoint; all TxLINE calls authenticate via one client with safe 401 refresh and no credential leakage.
</success_criteria>

<output>
Create `.planning/workstreams/backend/phases/02-feed-ingest-replay-state-machine/02-04-SUMMARY.md` when done.
</output>
