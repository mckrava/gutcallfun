---
phase: 01-foundation-data-layer
plan: 3
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/gutcallfun-core/src/models/game/enums.ts
  - apps/gutcallfun-core/src/models/account/user.entity.ts
  - apps/gutcallfun-core/src/models/account/user-score-profile.entity.ts
  - apps/gutcallfun-core/src/models/squad/squad.entity.ts
  - apps/gutcallfun-core/src/models/squad/squad-participant.entity.ts
  - apps/gutcallfun-core/src/models/squad/squad-score-profile.entity.ts
  - apps/gutcallfun-core/src/models/game/game.entity.ts
  - apps/gutcallfun-core/src/models/game/game-event.entity.ts
  - apps/gutcallfun-core/src/models/game/game-question-outcome.entity.ts
  - apps/gutcallfun-core/src/models/game/game-question.entity.ts
  - apps/gutcallfun-core/src/models/game/game-question-option.entity.ts
  - apps/gutcallfun-core/src/models/game/user-game.entity.ts
  - apps/gutcallfun-core/src/models/game/user-game-answer.entity.ts
  - apps/gutcallfun-core/src/db/migrations/InitialSchema.ts
autonomous: true
requirements: [DATA-01]
user_setup: []

must_haves:
  truths:
    - "The initial migration is a near-literal transcription of initial-request-src/initial-db-structure.sql: 3 hand-named CREATE TYPE enums, 12 CREATE TABLE statements, all 17 FK constraints, both partial unique indexes, all plain uniques, and the 3 btree indexes — in the SQL file's own statement order."
    - "The migration seeds game_question_outcome's 4 rows (fizzles=1, danger=2, shot=3, goal=4) in the same migration, immediately after that table's CREATE TABLE."
    - "Each of the 12 entities describes (does not generate) its SQL table 1:1 — matching column names, types, defaults, nullability, and PK shape."
    - "game.status/game_question.state/game_question.question_type map to the hand-named Postgres enums via enumName; the two partial unique indexes are declared with synchronize:false so the entities document (not manage) them."
  artifacts:
    - apps/gutcallfun-core/src/db/migrations/InitialSchema.ts
    - apps/gutcallfun-core/src/models/game/enums.ts
    - apps/gutcallfun-core/src/models/game/game.entity.ts
    - apps/gutcallfun-core/src/models/game/game-question.entity.ts
  key_links:
    - "game_question_outcome must be seeded before game_question_option's fk_gqo_outcome FK references it (FK ordering) — the ALTER TABLE FK block runs after all tables + the seed."
    - "enumName ties each enum column to exactly one shared Postgres type (each of the 3 enums is used by exactly one column — no cross-column duplication)."
  prohibitions:
    - statement: "MUST NOT alter, recalibrate, or deviate the LOCKED game_question_outcome seed contract — the outcome keys (fizzles, danger, shot, goal) and their ladder positions (1, 2, 3, 4) are frozen; no economy, point, or ladder value in the schema/seed may differ from the calibrated contract."
      status: unverified
      flagged: true
    - statement: "MUST NOT drop, weaken, or reduce to performance-only the DB-level correctness constraints — the partial unique indexes uq_game_fixture_live and uq_gq_one_open_per_game and the unique constraints uq_game_event_seq and uq_uga_user_question are integrity backstops, not optional."
      status: unverified
      flagged: true
---

<objective>
Author the authoritative initial migration (raw SQL transcribed 1:1 from `initial-request-src/initial-db-structure.sql`, including the `game_question_outcome` seed) and the 12 TypeORM entities that describe that schema. This is the whole of DATA-01.

Purpose: The SQL file is the LOCKED source of truth (CLAUDE.md). The raw-SQL-first migration owns schema authorship; the entities are a typed read of it so that `migration:generate` (verified in plan 04) computes an empty diff. Getting the byte-exact schema right is this phase's entire risk surface.
Output: `src/db/migrations/InitialSchema.ts`, `src/models/game/enums.ts`, and 12 `*.entity.ts` files.

## Artifacts this phase produces (this plan)
- Enums `GameStatus`, `QuestionState`, `QuestionType` in `src/models/game/enums.ts`
- Migration class `InitialSchema{timestamp}` (implements MigrationInterface) in `src/db/migrations/InitialSchema.ts`
- 12 entity classes: `UserEntity`, `UserScoreProfileEntity`, `SquadEntity`, `SquadParticipantEntity`, `SquadScoreProfileEntity`, `GameEntity`, `GameEventEntity`, `GameQuestionOutcomeEntity`, `GameQuestionEntity`, `GameQuestionOptionEntity`, `UserGameEntity`, `UserGameAnswerEntity`
</objective>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Account + squad entities (5 tables, describe-not-generate)</name>
  <files>apps/gutcallfun-core/src/models/account/user.entity.ts, apps/gutcallfun-core/src/models/account/user-score-profile.entity.ts, apps/gutcallfun-core/src/models/squad/squad.entity.ts, apps/gutcallfun-core/src/models/squad/squad-participant.entity.ts, apps/gutcallfun-core/src/models/squad/squad-score-profile.entity.ts</files>
  <read_first>
    - initial-request-src/initial-db-structure.sql (AUTHORITATIVE — read the `user` (25-35), `user_score_profile` (37-43), `squad` (45-55), `squad_participant` (57-65), `squad_score_profile` (67-73) blocks and the relevant UNIQUE constraints on lines 218-221)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (Entities section: PK-shape mapping rules — uuid gen_random_uuid → @PrimaryGeneratedColumn('uuid'), composite PK → two @PrimaryColumn, etc.)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 4, Don't Hand-Roll UUID row)
  </read_first>
  <action>
    Create five entity files under `src/models/account/` and `src/models/squad/`, each a typed 1:1 description of its SQL CREATE TABLE. Column names, types, defaults, and nullability must match the SQL exactly — describe, do not add anything absent from the SQL.
    - `user.entity.ts` (`UserEntity`, table `user`): id uuid PK default gen_random_uuid() → `@PrimaryGeneratedColumn('uuid')`; wallet_address varchar NOT NULL; share_code varchar(12) NOT NULL; handle varchar NOT NULL; image varchar nullable; score_profile varchar nullable; created_at timestamptz NOT NULL default now(); updated_at timestamptz nullable. Declare the three UNIQUE constraints from SQL 218-220 (`uq_user_wallet` on wallet_address, `uq_user_handle` on handle, `uq_user_share_code` on share_code) via `@Index(name, [col], { unique: true })` or column `unique: true`.
    - `user-score-profile.entity.ts` (`UserScoreProfileEntity`, table `user_score_profile`): id varchar PK (`@PrimaryColumn`); total_points bigint NOT NULL default 0; games_played int NOT NULL default 0; updated_at timestamptz nullable. (bigint maps to string in TypeORM — use `type: 'bigint'`.)
    - `squad.entity.ts` (`SquadEntity`, table `squad`): id int PK (plain `@PrimaryColumn('int')` — the SQL is `int NOT NULL`, NOT identity); name varchar NOT NULL; image varchar nullable; invite_code varchar nullable (UNIQUE `uq_squad_invite_code`, SQL 221); active boolean NOT NULL default true; created_at timestamptz NOT NULL default now(); updated_at timestamptz nullable; deleted_at timestamptz nullable.
    - `squad-participant.entity.ts` (`SquadParticipantEntity`, table `squad_participant`): composite PK (squad_id int, user_id uuid) → two `@PrimaryColumn`; active boolean NOT NULL default true; score_profile varchar nullable; created_at timestamptz NOT NULL default now(); deleted_at timestamptz nullable.
    - `squad-score-profile.entity.ts` (`SquadScoreProfileEntity`, table `squad_score_profile`): id varchar PK; total_points bigint NOT NULL default 0; games_played int NOT NULL default 0; updated_at timestamptz nullable.
    Use snake_case column names via `@Column({ name: '...' })` (or a snake_case naming strategy) so entity property camelCase maps to the SQL's snake_case columns exactly. Do NOT add FK relation decorators that would alter generated DDL — raw FK columns are the SQL-authoritative part; relations are optional and must not change column shape.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && ls src/models/account/user.entity.ts src/models/account/user-score-profile.entity.ts src/models/squad/squad.entity.ts src/models/squad/squad-participant.entity.ts src/models/squad/squad-score-profile.entity.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE 'account/|squad/' | grep -i error && echo HASERR || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist under `src/models/account/` and `src/models/squad/`.
    - `user.entity.ts` contains `@Entity('user')`, `@PrimaryGeneratedColumn('uuid')`, and `wallet_address` mapping with a unique constraint.
    - `squad-participant.entity.ts` declares two `@PrimaryColumn` (composite PK), and `squad.entity.ts` PK is a plain int column (no identity/generated).
    - `user-score-profile.entity.ts` and `squad-score-profile.entity.ts` use `type: 'bigint'` for total_points.
    - `npx tsc --noEmit` reports no errors originating in `models/account/` or `models/squad/`.
  </acceptance_criteria>
  <done>Five account/squad entities describe their SQL tables 1:1 (correct PK shapes, uniques, bigint), compiling clean.</done>
</task>

<task type="auto">
  <name>Task 2: Enums + game-aggregate entities (7 tables, enumName + partial indexes)</name>
  <files>apps/gutcallfun-core/src/models/game/enums.ts, apps/gutcallfun-core/src/models/game/game.entity.ts, apps/gutcallfun-core/src/models/game/game-event.entity.ts, apps/gutcallfun-core/src/models/game/game-question-outcome.entity.ts, apps/gutcallfun-core/src/models/game/game-question.entity.ts, apps/gutcallfun-core/src/models/game/game-question-option.entity.ts, apps/gutcallfun-core/src/models/game/user-game.entity.ts, apps/gutcallfun-core/src/models/game/user-game-answer.entity.ts</files>
  <read_first>
    - initial-request-src/initial-db-structure.sql (AUTHORITATIVE — read enums lines 13/14/19, and tables: game (78-104), game_event (106-120), game_question_outcome (125-131), game_question (133-149), game_question_option (151-160), user_game (165-171), user_game_answer (173-187); plus the partial indexes lines 223 & 237 and uniques 226/229/232/233/234)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (Enum column pattern: enumName + @Index synchronize:false partial-index shape, verbatim)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 4, Pitfall 1 partial-index synchronize:false, Pitfall 2 single-column-per-enum)
    - apps/gutcallfun-core/src/models/game/enums.ts (create it in this task if not present)
  </read_first>
  <action>
    Create `src/models/game/enums.ts` exporting three string enums whose members equal the SQL enum values exactly: `GameStatus` (scheduled, live, finished, cancelled), `QuestionState` (open, pending_confirmation, resolved, voided), `QuestionType` (attack_outcome, static).
    Create seven entity files under `src/models/game/`, each 1:1 with its SQL table:
    - `game.entity.ts` (`GameEntity`, table `game`): id int PK `GENERATED ALWAYS AS IDENTITY` → `@PrimaryGeneratedColumn('identity')` (the ONLY identity PK); status column `@Column({ type: 'enum', enum: GameStatus, enumName: 'game_status', default: GameStatus.SCHEDULED })`; all remaining columns per SQL 78-104 (fixture_id int NOT NULL, starts_at timestamptz nullable, participant1_id/participant2_id int nullable, participant1_is_home boolean NOT NULL default true, team1_name/team2_name/competition varchar nullable, fixture_group_id int nullable, team1_jersey_color/team2_jersey_color varchar nullable, current_status_id smallint nullable, score_p1/score_p2 smallint NOT NULL default 0, is_replay boolean NOT NULL default false, stream_cursor varchar nullable, stream_cursor_at timestamptz nullable, created_at timestamptz NOT NULL default now(), updated_at timestamptz nullable). Decorate the class with the partial unique index `@Index('uq_game_fixture_live', ['fixtureId'], { unique: true, where: 'NOT "is_replay"', synchronize: false })`.
    - `game-event.entity.ts` (`GameEventEntity`, table `game_event`): id uuid PK default gen_random_uuid(); game_id int NOT NULL; type varchar NOT NULL; payload jsonb NOT NULL (`type: 'jsonb'`); action_id int nullable; seq int NOT NULL; confirmed boolean nullable; participant smallint nullable; status_id smallint nullable; feed_ts timestamptz NOT NULL; received_at timestamptz NOT NULL default now(). Declare UNIQUE `uq_game_event_seq` on (game_id, seq) (SQL 226); the two plain indexes `idx_ge_action_id` on (game_id, action_id) and `idx_ge_type` on (game_id, type) may be declared as `@Index` (non-unique).
    - `game-question-outcome.entity.ts` (`GameQuestionOutcomeEntity`, table `game_question_outcome`): key varchar PK (`@PrimaryColumn`); content varchar NOT NULL; ladder_position smallint NOT NULL with UNIQUE `uq_gqoc_ladder` (SQL 233).
    - `game-question.entity.ts` (`GameQuestionEntity`, table `game_question`): id uuid PK default gen_random_uuid(); game_id int NOT NULL; trigger_event_id uuid nullable; resolution_event_id uuid nullable; question_type column `@Column({ type: 'enum', enum: QuestionType, enumName: 'question_type', default: QuestionType.ATTACK_OUTCOME })`; content varchar NOT NULL; participant smallint nullable; state column `@Column({ type: 'enum', enum: QuestionState, enumName: 'question_state', default: QuestionState.OPEN })`; resolved_option_id uuid nullable; answer_window_ttl int NOT NULL default 5; expires_at timestamptz NOT NULL; created_at timestamptz NOT NULL default now(); resolved_at timestamptz nullable. Decorate the class with the partial unique index `@Index('uq_gq_one_open_per_game', ['gameId'], { unique: true, where: `"state" = 'open'`, synchronize: false })`.
    - `game-question-option.entity.ts` (`GameQuestionOptionEntity`, table `game_question_option`): id uuid PK default gen_random_uuid(); game_question_id uuid NOT NULL; outcome_key varchar NOT NULL; base_gain int NOT NULL; display_order int NOT NULL. Declare uniques `uq_gqo_outcome` on (game_question_id, outcome_key) (SQL 232) and `uq_gqo_order` on (game_question_id, display_order) (SQL 234).
    - `user-game.entity.ts` (`UserGameEntity`, table `user_game`): composite PK (game_id int, user_id uuid) → two `@PrimaryColumn`; squad_id int nullable; joined_at timestamptz NOT NULL default now().
    - `user-game-answer.entity.ts` (`UserGameAnswerEntity`, table `user_game_answer`): id uuid PK default gen_random_uuid(); user_id uuid NOT NULL; game_id int NOT NULL; game_question_id uuid NOT NULL; selected_option_id uuid NOT NULL; reward_multiplier numeric(6,3) NOT NULL default 1 (`type: 'numeric', precision: 6, scale: 3`); awarded_points int nullable; successful_outcome boolean nullable; created_at timestamptz NOT NULL default now(); resolved_at timestamptz nullable. Declare UNIQUE `uq_uga_user_question` on (user_id, game_question_id) (SQL 229) and index `idx_uga_leaderboard` on (game_id, user_id) (SQL 244).
    Map every property to its snake_case SQL column via `@Column({ name })` (or naming strategy). Each of the 3 enums is used by exactly ONE column — do not reuse an enumName across columns (Pitfall 2).
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && grep -q "enumName: 'game_status'" src/models/game/game.entity.ts && grep -q "enumName: 'question_state'" src/models/game/game-question.entity.ts && grep -q "uq_gq_one_open_per_game" src/models/game/game-question.entity.ts && grep -q "synchronize: false" src/models/game/game.entity.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'models/game' | grep -i error && echo HASERR || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/models/game/enums.ts` exports `GameStatus`, `QuestionState`, `QuestionType` with members equal to the SQL enum string values.
    - `game.entity.ts` contains `@PrimaryGeneratedColumn('identity')`, `enumName: 'game_status'`, and `@Index('uq_game_fixture_live'` with `where: 'NOT "is_replay"'` and `synchronize: false`.
    - `game-question.entity.ts` contains `enumName: 'question_state'`, `enumName: 'question_type'`, and `@Index('uq_gq_one_open_per_game'` with `where: `"state" = 'open'`` and `synchronize: false`.
    - `game-event.entity.ts` uses `type: 'jsonb'` for payload and declares `uq_game_event_seq` on (game_id, seq).
    - `user-game-answer.entity.ts` declares `uq_uga_user_question` on (user_id, game_question_id) and uses numeric(6,3) for reward_multiplier.
    - `npx tsc --noEmit` reports no errors in `models/game/`.
  </acceptance_criteria>
  <done>Three enums + seven game entities describe their SQL tables 1:1 with enumName-bound enums and synchronize:false partial indexes; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Raw-SQL InitialSchema migration + game_question_outcome seed</name>
  <files>apps/gutcallfun-core/src/db/migrations/InitialSchema.ts</files>
  <read_first>
    - initial-request-src/initial-db-structure.sql (AUTHORITATIVE, all 244 lines — the migration is a near-literal transcription in this file's statement order)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (Migration section: exact transcription order 1-6, seed placement, down() reverse-order)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 3 raw-SQL migration, Anti-Patterns: no CREATE EXTENSION pgcrypto, seed in-migration)
    - apps/gutcallfun-core/src/db/data-source.ts (migrations glob picks up this file — plan 02)
  </read_first>
  <action>
    Create `src/db/migrations/InitialSchema.ts` with a class `InitialSchema{timestamp}` (use a real epoch millis for {timestamp}, e.g. the value returned by Date.now() at authoring time, and set the class `name` property to the same `InitialSchema{timestamp}` string so TypeORM records it). Implement `MigrationInterface` with `up(queryRunner)` and `down(queryRunner)`.
    In `up`, issue `queryRunner.query(...)` calls transcribed 1:1 from the SQL file, in the SQL file's own statement order:
    1. The 3 `CREATE TYPE ... AS ENUM (...)` statements (game_status, question_state, question_type) exactly as SQL lines 13/14/19.
    2. The 12 `CREATE TABLE "public"."..."` statements in SQL order: user, user_score_profile, squad, squad_participant, squad_score_profile, game, game_event, game_question_outcome, game_question, game_question_option, user_game, user_game_answer — every column name/type/default/nullability byte-matching the SQL (game.id uses `GENERATED ALWAYS AS IDENTITY`; uuid PKs use `DEFAULT gen_random_uuid()`; timestamptz defaults use `now()`).
    3. Immediately after the game_question_outcome CREATE TABLE, INSERT its 4 seed rows: (key, content, ladder_position) = ('fizzles','Fizzles out',1), ('danger','Creates danger',2), ('shot','Shot taken',3), ('goal','GOAL!',4). These values are the LOCKED economy contract — transcribe exactly, do not alter keys or ladder positions.
    4. The 17 `ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY` statements (SQL 192-213) in order.
    5. The UNIQUE constraints and both partial unique indexes (SQL 218-234): the plain uniques as `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE`, plus `CREATE UNIQUE INDEX "uq_game_fixture_live" ON "public"."game" ("fixture_id") WHERE NOT "is_replay"` and `CREATE UNIQUE INDEX "uq_gq_one_open_per_game" ON "public"."game_question" ("game_id") WHERE "state" = 'open'`.
    6. The 3 plain `CREATE INDEX` statements (SQL 240-244): idx_ge_action_id, idx_ge_type, idx_uga_leaderboard.
    Do NOT add `CREATE EXTENSION pgcrypto` — gen_random_uuid() is a core built-in on Postgres 16 and is not in the authoritative SQL.
    In `down`, drop everything in reverse dependency order (indexes → uniques → FKs → tables → types) so the migration is cleanly revertible. Use static hardcoded SQL literals only — never interpolate external input.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && test $(grep -c 'CREATE TABLE' src/db/migrations/InitialSchema.ts) -ge 12 && grep -c 'CREATE TYPE' src/db/migrations/InitialSchema.ts && grep -q 'uq_game_fixture_live' src/db/migrations/InitialSchema.ts && grep -q 'uq_gq_one_open_per_game' src/db/migrations/InitialSchema.ts && grep -q "'fizzles'" src/db/migrations/InitialSchema.ts && grep -q "'goal'" src/db/migrations/InitialSchema.ts && grep -vi 'CREATE EXTENSION' src/db/migrations/InitialSchema.ts >/dev/null && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'migrations/' | grep -i error && echo HASERR || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/migrations/InitialSchema.ts` implements `MigrationInterface` with `up` and `down`; class name and `name` property match `InitialSchema{timestamp}`.
    - Contains exactly 3 `CREATE TYPE` and at least 12 `CREATE TABLE` calls in SQL-file order.
    - Contains both `CREATE UNIQUE INDEX "uq_game_fixture_live"` (WHERE NOT "is_replay") and `CREATE UNIQUE INDEX "uq_gq_one_open_per_game"` (WHERE "state" = 'open').
    - Seeds game_question_outcome with the four rows fizzles/1, danger/2, shot/3, goal/4 (economy contract unaltered).
    - Contains no `CREATE EXTENSION` statement.
    - `npx tsc --noEmit` reports no errors in `db/migrations/`.
  </acceptance_criteria>
  <done>The InitialSchema migration transcribes the authoritative SQL 1:1 (enums, 12 tables, FKs, both partial indexes, uniques, indexes) and seeds the locked outcome rows; compiles clean and is revertible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL text → live schema | DDL authorship crosses into the database schema |
| seed values → economy/leaderboard correctness | The seeded ladder positions define partial-credit adjacency downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering (SQL injection) | migration up()/down() | low | mitigate | Seed and DDL are static hardcoded literals — no external/user input is interpolated into any queryRunner.query string |
| T-01-08 | Tampering | LOCKED economy seed | high | mitigate | game_question_outcome keys + ladder positions transcribed exactly from the calibrated contract; prohibition guards against deviation (n=1,708 calibration is frozen) |
| T-01-09 | Tampering | DB correctness constraints | high | mitigate | Partial unique indexes (one-open-window, one-live-game-per-fixture) and uniques (one-answer-per-user-per-question, idempotent event seq) transcribed as raw SQL — DB-level backstops, not app-only |
</threat_model>

<verification>
- `grep -c 'CREATE TABLE' src/db/migrations/InitialSchema.ts` → >= 12; `grep -c 'CREATE TYPE'` → 3.
- Both partial-index names present in the migration and both declared with synchronize:false in game.entity.ts / game-question.entity.ts.
- Seed rows fizzles/danger/shot/goal with ladder 1-4 present.
- `npx tsc --noEmit` clean across models/ and migrations/.
- (Empty-diff and live-DB assertions are executed in plan 04 after the schema is pushed.)
</verification>

<success_criteria>
DATA-01 satisfied at the source level: the raw-SQL migration reproduces the authoritative schema 1:1 with the locked seed, and 12 entities describe it (enumName-bound enums, synchronize:false partial indexes) — all compiling clean, ready for the plan-04 empty-diff gate.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md` when done.
</output>
