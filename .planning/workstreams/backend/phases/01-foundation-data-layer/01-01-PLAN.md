---
phase: 01-foundation-data-layer
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gutcallfun-core/package.json
  - apps/gutcallfun-core/.env.example
  - apps/gutcallfun-core/src/config/app-config.schema.ts
  - apps/gutcallfun-core/src/config/config.module.ts
  - apps/gutcallfun-core/src/config/app-config.schema.spec.ts
  - docker-compose.yml
autonomous: false
requirements: [DATA-02, DATA-03, DATA-04]
user_setup: []

must_haves:
  truths:
    - "Running the app (or the config validate function) with a missing/malformed DATABASE_URL aborts with a class-validator error before any DB connection is attempted — the process never starts half-configured."
    - "`npm --workspace=apps/gutcallfun-core ls typeorm` resolves to 0.3.31, not the 1.x line."
    - "`docker compose up -d db` brings the Postgres 16 service to healthy state using `pg_isready -U gutcallfun -d gutcallfun` (no stale pmmarkets values)."
    - "package.json exposes migration:generate / migration:run / migration:revert scripts wired to src/db/data-source.ts."
  artifacts:
    - apps/gutcallfun-core/src/config/app-config.schema.ts
    - apps/gutcallfun-core/src/config/config.module.ts
    - apps/gutcallfun-core/src/config/app-config.schema.spec.ts
    - apps/gutcallfun-core/.env.example
  key_links:
    - "AppConfigModule.validate (synchronous validateSync) runs inside ConfigModule.forRoot at module instantiation, so it fails before any injected-ConfigService DB factory executes (Pitfall 3)."
  prohibitions:
    - statement: "MUST NOT silently substitute defaults for missing or invalid correctness-critical env vars (DATABASE_URL, PORT, NODE_ENV) — boot MUST fail fast via validateSync, never start in a half-configured state."
      status: unverified
      flagged: true
---

<objective>
Establish the bootable foundation: install the LOCKED pinned dependency set (TypeORM 0.3.31 and family), add the tsx-driven migration CLI scripts, fix the docker-compose Postgres 16 healthcheck, create `.env.example`, and build the `@Global()` fail-fast config-validation module.

Purpose: Every later plan and phase depends on (a) the correct pinned toolchain being installed, (b) a healthy local Postgres 16, and (c) an app that refuses to boot on bad config. This plan delivers DATA-02 (fail-fast config) and the dependency/docker halves of DATA-03 and DATA-04.
Output: Installed deps + migration scripts, fixed healthcheck, `.env.example`, and `src/config/*` validation module with a passing fail-fast spec.

## Artifacts this phase produces (this plan)
- Class `EnvironmentVariables` (config env DTO) in `src/config/app-config.schema.ts`
- Function `validate(config)` (fail-fast env validator) in `src/config/app-config.schema.ts`
- Class `AppConfigModule` (`@Global()`) in `src/config/config.module.ts`
- Spec `src/config/app-config.schema.spec.ts`
- New file `apps/gutcallfun-core/.env.example`
- package.json scripts: `migration:generate`, `migration:run`, `migration:revert`
- Modified `docker-compose.yml` healthcheck

<flagged_assumptions>
- [FLAGGED ASSUMPTION — edge probe DATA-02 "unclassified", unresolved] The edge probe did not classify env-validation behavior under an interrupted/partial boot. Working assumption: `validateSync` is synchronous and total (all-or-nothing) so there is no partial-config boot state; confirm during execution that a missing DATABASE_URL throws before the TypeOrm factory runs. Do not silently drop this assumption.
- [CANON-REFERRAL — dropped, not minted] "MUST NOT commit real DB / TxLINE credentials to git" is canon secrets-management (Information Disclosure) — covered by `.gitignore` (`.env` ignored), the threat model (T-01-02), and `/gsd-secure-phase`; not minted as a bespoke prohibition here.
</flagged_assumptions>
</objective>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (pre-install version confirm)</name>
  <files>apps/gutcallfun-core/package.json</files>
  <action>HALT before any dependency install. This is a blocking human-verify gate: the human confirms the pinned versions in the RESEARCH.md Package Legitimacy Audit still resolve on the npm registry (per the how-to-verify steps below) before Task 2 installs them. Do not auto-approve — 7 of 10 packages carry a `SUS (too-new)` verdict (assessed false-positive; official repos, 2.8M–73M weekly downloads, no postinstall scripts), so a human must eyeball the versions once. typeorm MUST resolve to 0.3.31, never the 1.x line.</action>
  <what-built>Nothing yet — this is a pre-install verification gate. The RESEARCH.md Package Legitimacy Audit flagged 7 of 10 packages as `SUS (too-new)`, assessed as false positives (all are 2.8M–73M weekly-download mainstream packages on official repos with no postinstall scripts). Per the package-legitimacy protocol, confirm the pinned versions still resolve on the registry before installing.</what-built>
  <how-to-verify>
    1. From `apps/gutcallfun-core`, run: `npm view typeorm@0.3.31 version` and confirm it prints `0.3.31` (NOT 1.x).
    2. Run `npm view @nestjs/typeorm@11.0.3 version`, `npm view pg@8.22.0 version`, `npm view @nestjs/config@4.0.4 version`, `npm view class-validator@0.15.1 version`, `npm view class-transformer@0.5.1 version`, `npm view tsx@4.23.1 version` — each must print the pinned version.
    3. Spot-check `npm view typeorm@0.3.31 scripts.postinstall` returns empty/undefined (no install-time script).
    4. Confirm none of these resolve to an unexpected major (especially typeorm must be 0.3.31, never 1.x — 1.x has breaking where-condition semantics per CLAUDE.md).
  </how-to-verify>
  <resume-signal>Type "approved" if all versions match the audit table, or describe any mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install pinned deps, add migration scripts, fix docker healthcheck, create .env.example</name>
  <files>apps/gutcallfun-core/package.json, docker-compose.yml, apps/gutcallfun-core/.env.example</files>
  <read_first>
    - apps/gutcallfun-core/package.json (current boilerplate deps + scripts — read before editing)
    - docker-compose.yml (repo root — the stale `pmmarkets` healthcheck lives here, NOT in apps/gutcallfun-core; port binding 127.0.0.1:5488:5432 must be preserved)
    - apps/gutcallfun-core/.env.example (currently empty)
    - apps/gutcallfun-core/tsconfig.json (module: nodenext — affects data-source import shape referenced by scripts)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Standard Stack install block, Code Examples package.json scripts, Pitfall 5 healthcheck)
  </read_first>
  <action>
    Install the exact pinned versions into `apps/gutcallfun-core` (workspace install): dependencies `@nestjs/typeorm@11.0.3`, `typeorm@0.3.31`, `pg@8.22.0`, `@nestjs/config@4.0.4`, `class-validator@0.15.1`, `class-transformer@0.5.1`; devDependency `tsx@4.23.1`. Use `npm install --workspace=apps/gutcallfun-core <pkg>@<version>` so versions land pinned (no caret drift) — typeorm MUST stay at 0.3.31.
    Add three scripts to `apps/gutcallfun-core/package.json`: `migration:generate` = `tsx ./node_modules/typeorm/cli.js migration:generate -d src/db/data-source.ts src/db/migrations/Migration`, `migration:run` = `tsx ./node_modules/typeorm/cli.js migration:run -d src/db/data-source.ts`, `migration:revert` = `tsx ./node_modules/typeorm/cli.js migration:revert -d src/db/data-source.ts`.
    Fix the `docker-compose.yml` healthcheck at repo root: change the `test:` command from the stale `pg_isready -U pmmarkets -d pmmarkets` to `pg_isready -U gutcallfun -d gutcallfun` (matching the `environment:` block's POSTGRES_USER/POSTGRES_DB=gutcallfun). Do NOT change the `ports:` binding `127.0.0.1:5488:5432` (loopback-only, deliberate — Security Domain).
    Populate `apps/gutcallfun-core/.env.example` with placeholder (non-secret) keys the app and CLI read: `DATABASE_URL=postgresql://gutcallfun:changeme-local-only@127.0.0.1:5488/gutcallfun`, `PORT=3000`, `NODE_ENV=development`. Use placeholder values only — never real credentials.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npm ls typeorm | grep -q '0.3.31' && npm run | grep -q 'migration:run' && grep -q 'gutcallfun' ../../docker-compose.yml && grep -qi 'DATABASE_URL' .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/gutcallfun-core/package.json` dependencies contain `"typeorm": "0.3.31"` (exact, no caret) and `"@nestjs/typeorm": "11.0.3"`, `"pg": "8.22.0"`, `"@nestjs/config": "4.0.4"`, `"class-validator": "0.15.1"`, `"class-transformer": "0.5.1"`; devDependencies contain `"tsx": "4.23.1"`.
    - `npm --workspace=apps/gutcallfun-core ls typeorm` prints `typeorm@0.3.31`.
    - package.json scripts include `migration:generate`, `migration:run`, `migration:revert` referencing `src/db/data-source.ts`.
    - `docker-compose.yml` healthcheck `test:` contains `pg_isready -U gutcallfun -d gutcallfun` and no longer contains `pmmarkets`; `ports:` still contains `127.0.0.1:5488:5432`.
    - `apps/gutcallfun-core/.env.example` contains `DATABASE_URL=`, `PORT=`, `NODE_ENV=` with placeholder values only.
  </acceptance_criteria>
  <done>Pinned toolchain installed (typeorm 0.3.31), migration scripts present, docker healthcheck corrected to gutcallfun, .env.example seeded with placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-fast @Global() config validation module</name>
  <files>apps/gutcallfun-core/src/config/app-config.schema.ts, apps/gutcallfun-core/src/config/config.module.ts, apps/gutcallfun-core/src/config/app-config.schema.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (Pattern Assignments: app-config.schema.ts + config.module.ts verbatim shapes; DATABASE_URL single-var decision)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 1 fail-fast config, Pitfall 3 ordering, Security Domain V5)
    - apps/gutcallfun-core/tsconfig.json (strictNullChecks true, emitDecoratorMetadata true)
  </read_first>
  <behavior>
    - Test 1: `validate({ DATABASE_URL: 'postgresql://u:p@127.0.0.1:5488/db', PORT: '3000', NODE_ENV: 'development' })` returns a validated instance (no throw).
    - Test 2: `validate({ PORT: '3000', NODE_ENV: 'development' })` (DATABASE_URL missing) throws.
    - Test 3: `validate({ DATABASE_URL: 'not-a-postgres-url', PORT: '3000' })` throws (invalid protocol).
    - Test 4: extra unknown keys already in `.env` (e.g. a TxLINE token) do NOT cause a throw — validation does not reject non-whitelisted keys.
  </behavior>
  <action>
    Create `src/config/app-config.schema.ts` exporting a class `EnvironmentVariables` decorated with class-validator, and a synchronous `validate(config: Record<string, unknown>)` function that runs `plainToInstance(EnvironmentVariables, config, { enableImplicitConversion: true })` then `validateSync(instance, { skipMissingProperties: false })` and throws `new Error(errors.toString())` when errors exist (fail fast — no default fallback for correctness-critical vars). Fields: `DATABASE_URL` required, validated with `@IsUrl({ protocols: ['postgresql','postgres'], require_tld: false })`; `PORT` `@IsInt() @Min(0) @Max(65535)` and optional with a default of 3000; `NODE_ENV` `@IsString()` optional defaulting to 'development'. Do NOT set `forbidNonWhitelisted`/`whitelist` — extra env vars (TxLINE creds present in `.env` for later phases) must pass through untouched.
    Create `src/config/config.module.ts` exporting `@Global()` class `AppConfigModule` whose `imports` is `[ConfigModule.forRoot({ isGlobal: true, validate })]` and which re-exports `ConfigModule`. Keep `validate` synchronous so it completes during module instantiation before any injected-ConfigService DB factory runs (Pitfall 3).
    Create `src/config/app-config.schema.spec.ts` implementing the four behavior cases above against the exported `validate` function.
  </action>
  <verify>
    <automated>cd apps/gutcallfun-core && npx jest src/config/app-config.schema.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/app-config.schema.ts` contains `export function validate(` and `validateSync(` and `plainToInstance(`.
    - `src/config/config.module.ts` contains `@Global()` and `ConfigModule.forRoot({` with `validate`.
    - `npx jest src/config/app-config.schema.spec.ts` exits 0 with all four cases passing (missing DATABASE_URL throws; valid config returns instance; extra keys do not throw).
    - The word default does not appear as a fallback value assignment for DATABASE_URL (DATABASE_URL has no default — it is required).
  </acceptance_criteria>
  <done>Config module validates env synchronously and fails fast on missing/invalid DATABASE_URL; spec proves the fail-fast and pass-through behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment (.env / process.env) → app boot | Untrusted/misconfigured env crosses into runtime config here |
| npm registry → local node_modules | Third-party package code enters the project (supply chain) |
| docker host → Postgres container | DB exposure surface (port binding) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering/DoS | env config → boot | medium | mitigate | Fail-fast `validateSync` in `validate()`; app aborts on missing/invalid DATABASE_URL rather than starting half-configured (DATA-02) |
| T-01-02 | Information Disclosure | DB/TxLINE credentials | high | mitigate | `.env` git-ignored (confirmed); `.env.example` carries placeholder values only, never real secrets |
| T-01-04 | Information Disclosure | Postgres port | medium | mitigate | Preserve `127.0.0.1:5488:5432` loopback-only binding in docker-compose; do not widen to 0.0.0.0 |
| T-01-SC | Tampering | npm installs (typeorm et al.) | high | mitigate | Package-legitimacy checkpoint (Task 1) confirms pinned versions on registry before install; typeorm hard-pinned to 0.3.31; no `[SLOP]` packages; SUS verdicts assessed false-positive (too-new) with official repos + no postinstall scripts |
</threat_model>

<verification>
- `npm --workspace=apps/gutcallfun-core ls typeorm` → `typeorm@0.3.31`.
- `npx jest src/config/app-config.schema.spec.ts` → all pass.
- `docker compose up -d db && docker compose ps` → db reports `healthy` within the retry window.
- `grep -c pmmarkets docker-compose.yml` → 0.
</verification>

<success_criteria>
Pinned toolchain installed (typeorm 0.3.31), tsx migration scripts present, docker-compose healthcheck references gutcallfun and reports healthy, `.env.example` seeded with placeholders, and the fail-fast config module rejects a missing/invalid DATABASE_URL (proven by spec).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md` when done.
</output>
