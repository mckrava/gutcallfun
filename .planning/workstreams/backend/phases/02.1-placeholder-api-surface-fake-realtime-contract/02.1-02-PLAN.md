---
phase: 02.1-placeholder-api-surface-fake-realtime-contract
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts
  - apps/gutcallfun-core/src/mocks/fixtures/squads.fixtures.ts
  - apps/gutcallfun-core/src/modules/api/users/dto/user-response.dto.ts
  - apps/gutcallfun-core/src/modules/api/users/dto/create-user.dto.ts
  - apps/gutcallfun-core/src/modules/api/users/dto/update-user.dto.ts
  - apps/gutcallfun-core/src/modules/api/users/dto/list-users-query.dto.ts
  - apps/gutcallfun-core/src/modules/api/users/dto/user-score-profile-response.dto.ts
  - apps/gutcallfun-core/src/modules/api/users/users.controller.ts
  - apps/gutcallfun-core/src/modules/api/users/users.service.ts
  - apps/gutcallfun-core/src/modules/api/users/users.service.spec.ts
  - apps/gutcallfun-core/src/modules/api/users/users.module.ts
  - apps/gutcallfun-core/src/modules/api/squads/dto/squad-response.dto.ts
  - apps/gutcallfun-core/src/modules/api/squads/dto/create-squad.dto.ts
  - apps/gutcallfun-core/src/modules/api/squads/dto/list-squads-query.dto.ts
  - apps/gutcallfun-core/src/modules/api/squads/dto/squad-participant-response.dto.ts
  - apps/gutcallfun-core/src/modules/api/squads/dto/create-squad-participant.dto.ts
  - apps/gutcallfun-core/src/modules/api/squads/dto/squad-score-profile-response.dto.ts
  - apps/gutcallfun-core/src/modules/api/squads/squads.controller.ts
  - apps/gutcallfun-core/src/modules/api/squads/squads.service.ts
  - apps/gutcallfun-core/src/modules/api/squads/squads.service.spec.ts
  - apps/gutcallfun-core/src/modules/api/squads/squads.module.ts
autonomous: true
requirements: [API-01, API-02]

must_haves:
  truths:
    - "GET /users/{user_id} returns HTTP 200 with a body whose keys are exactly id, wallet_address, share_code, handle, image, score_profile, created_at, updated_at — every key in snake_case, matching initial-db-structure.sql 1:1 (D-01)"
    - "Two identical GET /users requests return byte-identical response bodies — no Math.random() anywhere in the fixture or service path (D-04)"
    - "GET /users/{user_id} for an id that is a well-formed uuid but absent from the fixtures returns HTTP 404 with a structured body, never a 500 and never an empty 200"
    - "GET /users/{user_id} for a value that is not a well-formed uuid returns HTTP 400, never a 500"
    - "PATCH /users/{user_id} accepts only handle and image; a body additionally carrying wallet_address, id, or share_code is rejected with HTTP 400"
    - "GET /users?squad_id={id} for a squad with no members returns HTTP 200 with items as an empty array and total 0, never 404 and never null"
    - "POST /squads returns a squad carrying an explicitly-assigned integer id — squad.id is a PrimaryColumn with no database default, so no code path may assume the database will generate it"
    - "GET /squads/{squad_id}/score-profile returns the profile row that the squad's participants reference via squad_participant.score_profile, reflecting the reversed FK direction in the schema"
    - "Every DTO in this plan is registered in the OpenAPI document; GET /api-docs-json contains a components.schemas entry for UserResponseDto and SquadResponseDto"
  artifacts:
    - path: "apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts"
      provides: "Deterministic user + user_score_profile fixture constants"
    - path: "apps/gutcallfun-core/src/mocks/fixtures/squads.fixtures.ts"
      provides: "Deterministic squad, squad_participant, squad_score_profile fixture constants"
    - path: "apps/gutcallfun-core/src/modules/api/users/users.controller.ts"
      provides: "Five user routes"
      exports: ["UsersController"]
    - path: "apps/gutcallfun-core/src/modules/api/squads/squads.controller.ts"
      provides: "Six squad routes"
      exports: ["SquadsController"]
  key_links:
    - from: "apps/gutcallfun-core/src/modules/api/users/users.service.ts"
      to: "apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts"
      via: "service reads the static fixture constant instead of injecting a repository"
      pattern: "users.fixtures"
    - from: "apps/gutcallfun-core/src/modules/api/users/users.controller.ts"
      to: "apps/gutcallfun-core/src/modules/api/users/dto/user-response.dto.ts"
      via: "every handler declares a DTO class as its return type, never an entity"
      pattern: "UserResponseDto"
  prohibitions:
    - statement: "MUST NOT place real personal data in the fixtures — no real Solana wallet address belonging to an actual person, no real handle scraped from a live account, no real avatar URL pointing at a third party's image. Fixture identities must be obviously synthetic, because this file is committed to a public-facing repository and will be served unauthenticated from a deployed demo"
      category: privacy
    - statement: "MUST NOT present the mock surface in a way that leaves a UI developer or demo viewer unable to tell it apart from real data — the OpenAPI description states the surface is placeholder, and fixture values must be self-evidently synthetic rather than plausible-looking real records"
      category: transparency
---

<objective>
Freeze the account and squad half of the client contract: eleven REST routes covering users, user
score profiles, squads, squad participants, and squad score profiles, each returning a
realistically-shaped, deterministic mock payload whose field names and types match
`initial-db-structure.sql` exactly (D-01, D-04).

Purpose: the UI developer can build the profile screen, the teammates list, and the squad screens
today without inventing a single field name, and Phase 5 later swaps a repository in behind these
identical DTOs with zero UI rework (D-05).

Output: two fixture modules, eleven Swagger-documented routes, two mock-backed services with unit
specs. No repository injection, no auth, no persistence (D-02).
</objective>

<execution_context>
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/workflows/execute-plan.md
@/Users/maximkravchuk/Development/Projects/Solana/gutcall-project/gutcallfun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/02.1-CONTEXT.md
@.planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/02.1-RESEARCH.md
@.planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/02.1-PATTERNS.md
@.planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/COVERAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic account and squad fixtures</name>
  <files>apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts, apps/gutcallfun-core/src/mocks/fixtures/squads.fixtures.ts</files>
  <read_first>
    - initial-request-src/initial-db-structure.sql (tables `user`, `user_score_profile`, `squad`, `squad_participant`, `squad_score_profile` — the authoritative field names and types)
    - apps/gutcallfun-core/src/models/account/user.entity.ts (camelCase property ↔ snake_case column mapping to mirror in reverse)
    - apps/gutcallfun-core/src/models/squad/squad.entity.ts (confirm `id` uses PrimaryColumn, not PrimaryGeneratedColumn)
    - apps/gutcallfun-core/src/models/squad/squad-participant.entity.ts (composite PK shape)
    - .planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/02.1-RESEARCH.md (Pattern 1 — field casing; Pitfall 4 — squad.id has no DB default)
  </read_first>
  <action>
    Create `src/mocks/fixtures/users.fixtures.ts` exporting two frozen constants: `USERS_FIXTURE`
    (four users) and `USER_SCORE_PROFILES_FIXTURE` (three profiles; one user deliberately has a
    null `score_profile` because the column is nullable and the UI must handle that case).

    Each user object carries exactly these snake_case keys, matching the `user` table: `id` (uuid
    string), `wallet_address` (string), `share_code` (string in the `GC-XXXX-XXXX` Crockford form,
    12 chars), `handle` (string), `image` (string or null), `score_profile` (string or null),
    `created_at` (ISO 8601 string), `updated_at` (ISO 8601 string or null). Use fixed literal
    values — no `Date.now()`, no `crypto.randomUUID()`, no `Math.random()` (D-04). Hard-code the
    uuids and the timestamps so two requests a week apart return the same bytes.

    Fixture identities must be self-evidently synthetic. Use obviously-fake handles and wallet
    addresses that no real person owns; do not copy a real Solana pubkey from `.env`, from the
    TxODDS tooling, or from a block explorer. Avatar values should be a deterministic seed string
    rather than a URL pointing at someone else's hosted image.

    Each `user_score_profile` object carries `id` (varchar — this is the PK that `user.score_profile`
    points *at*, note the reversed FK direction), `total_points`, `games_played`, `updated_at`.
    `total_points` is a Postgres `bigint`; represent it as a JSON integer here and record in a
    file-head comment that the Phase 5 implementation must coerce the pg driver's string return
    into a number so the wire type does not silently change from integer to string.

    Create `src/mocks/fixtures/squads.fixtures.ts` exporting `SQUADS_FIXTURE` (two squads),
    `SQUAD_PARTICIPANTS_FIXTURE` (participants linking the fixture users to those squads, including
    one squad with zero participants so the empty-list path is exercisable), and
    `SQUAD_SCORE_PROFILES_FIXTURE`. Squad keys: `id` (integer), `name`, `image`, `invite_code`,
    `active`, `created_at`, `updated_at`, `deleted_at`. Squad participant keys: `squad_id`,
    `user_id`, `active`, `score_profile`, `created_at`, `deleted_at`.

    Export a `nextSquadId()` helper from the squads fixture module that returns
    `Math.max(...SQUADS_FIXTURE.map(s => s.id)) + 1`. `squad.id` is declared `@PrimaryColumn` with
    no database default, so squad creation must supply an id explicitly; deriving it from the fixed
    fixture set keeps it deterministic. Add a head comment recording that Phase 5 needs a real
    app-level id strategy (a Postgres sequence added by migration, or a max-plus-one query with a
    unique-violation retry loop) — the database will not assign this for you.

    Do not import anything from `src/modules/ingest/**` and do not import a TypeORM entity into
    these files.
  </action>
  <acceptance_criteria>
    - `grep -rc 'Math.random\|randomUUID\|Date.now' apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts apps/gutcallfun-core/src/mocks/fixtures/squads.fixtures.ts` reports 0 for both files
    - `grep -c 'wallet_address' apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts` returns at least 4
    - `grep -c 'nextSquadId' apps/gutcallfun-core/src/mocks/fixtures/squads.fixtures.ts` returns at least 1
    - `grep -rc "from '.*modules/ingest" apps/gutcallfun-core/src/mocks/` returns 0
    - `npm run build --workspace=apps/gutcallfun-core` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build</automated>
  </verify>
  <done>Both fixture modules compile, contain only literal deterministic values, hold no real personal data, and import nothing from the ingest module.</done>
</task>

<task type="auto">
  <name>Task 2: User DTOs, service, controller, module</name>
  <files>apps/gutcallfun-core/src/modules/api/users/dto/user-response.dto.ts, apps/gutcallfun-core/src/modules/api/users/dto/create-user.dto.ts, apps/gutcallfun-core/src/modules/api/users/dto/update-user.dto.ts, apps/gutcallfun-core/src/modules/api/users/dto/list-users-query.dto.ts, apps/gutcallfun-core/src/modules/api/users/dto/user-score-profile-response.dto.ts, apps/gutcallfun-core/src/modules/api/users/users.controller.ts, apps/gutcallfun-core/src/modules/api/users/users.service.ts, apps/gutcallfun-core/src/modules/api/users/users.service.spec.ts, apps/gutcallfun-core/src/modules/api/users/users.module.ts</files>
  <read_first>
    - initial-request-src/initial-db-structure.sql (table `user` and `user_score_profile`)
    - apps/gutcallfun-core/src/common/dto/pagination-query.dto.ts (shared limit/offset DTO from plan 01)
    - apps/gutcallfun-core/src/mocks/fixtures/users.fixtures.ts (created in Task 1)
    - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures.module.ts (module shape to copy)
    - apps/gutcallfun-core/src/modules/ingest/fixtures/fixtures-cron.service.ts (Logger + constructor-DI style to copy; ignore its repository injection)
    - .planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/02.1-RESEARCH.md (Pattern 1 field casing, Pattern 2 mock-backed service)
  </read_first>
  <action>
    Create the DTOs under `src/modules/api/users/dto/`. Every property name is declared literally
    in snake_case to match the SQL column names — do not copy the entity's camelCase property
    names, and do not reach for `@Expose`/`ClassSerializerInterceptor` indirection. Decorate every
    property with `@ApiProperty` or `@ApiPropertyOptional`, giving integer-typed properties an
    explicit `type: 'integer'` so the OpenAPI document does not document them as the looser
    `number`.

    - `UserResponseDto`: `id`, `wallet_address`, `share_code`, `handle`, `image`, `score_profile`,
      `created_at`, `updated_at`. Nullable columns are declared nullable in the Swagger metadata.
    - `CreateUserDto`: `wallet_address` (`@IsString()`, `@IsNotEmpty()`) and `handle`
      (`@IsString()`, length-bounded); `image` optional. Per D-02 wallet sign-in is bypassed
      entirely — the caller simply asserts a wallet address and no signature is verified. Add a
      Swagger description on the DTO saying so plainly, so the shape's provisional nature is
      visible in the published contract.
    - `UpdateUserDto`: exactly two optional properties, `handle` and `image`, and nothing else.
      This narrow declaration is what makes the global `forbidNonWhitelisted` pipe reject an
      attempt to smuggle `wallet_address`, `id`, `share_code`, or `score_profile` through the PATCH
      body.
    - `ListUsersQueryDto`: extends the shared `PaginationQueryDto`; optional `wallet_address`,
      `handle`, and `squad_id` filters. `squad_id` is `@Type(() => Number)` + `@IsInt()` because
      `squad.id` is an integer; the other two are optional strings.
    - `UserScoreProfileResponseDto`: `id`, `total_points` (`type: 'integer'`), `games_played`
      (`type: 'integer'`), `updated_at`.
    - `PaginatedUsersResponseDto`: concrete subclass of the shared paginated envelope declaring
      `items` as `UserResponseDto[]` so Swagger can resolve the array member type.

    Create `users.service.ts` following the mock-backed pattern: no `@InjectRepository`, no
    TypeORM import. Instantiate a logger as `new Logger(UsersService.name)` to match the codebase
    convention. Methods: `findAll(query)` filtering `USERS_FIXTURE` by whichever of
    `wallet_address`/`handle`/`squad_id` are present (the `squad_id` filter resolves through
    `SQUAD_PARTICIPANTS_FIXTURE` to produce that squad's members) and returning the paginated
    envelope with a `total` reflecting the filtered count before slicing; `findOne(userId)`
    throwing `NotFoundException` when absent; `create(dto)` returning a deterministic new user
    derived from the input plus fixed fixture-style defaults; `update(userId, dto)` returning the
    fixture user with the supplied fields overlaid, throwing `NotFoundException` when absent;
    `findScoreProfile(userId)` resolving the user first, then returning the profile whose `id`
    equals that user's `score_profile` value — this is the reversed FK direction, so a user with a
    null `score_profile` yields a `NotFoundException` rather than an empty object.

    Create `users.controller.ts` with `@Controller('users')` and `@ApiTags('users')`. Routes:
    `GET /users`, `GET /users/:user_id`, `POST /users`, `PATCH /users/:user_id`,
    `GET /users/:user_id/score-profile`. Bind the path param with `@Param('user_id', ParseUUIDPipe)`
    so a malformed uuid fails with a 400 before the handler body runs rather than falling through
    to a 500. Every handler declares a DTO class as its return type — never an entity type, never
    `any`. Add `@ApiOkResponse`/`@ApiCreatedResponse` with the concrete DTO type and
    `@ApiNotFoundResponse` where the service can throw, so the published contract documents the
    404 path.

    Create `users.module.ts` declaring `controllers: [UsersController]` and
    `providers: [UsersService]`, exporting `UsersService`. Copy the shape of
    `src/modules/ingest/fixtures/fixtures.module.ts` minus its `TypeOrmModule.forFeature` import —
    there is no repository this phase. Do not register this module anywhere yet; plan 05 owns all
    module registration so parallel plans never contend for `app.module.ts`.

    Create `users.service.spec.ts` using `Test.createTestingModule({ providers: [UsersService] })`.
    Assert: `findOne` on a known fixture id returns an object whose keys include `wallet_address`
    and `share_code`; `findOne` on an absent uuid throws `NotFoundException`; two successive
    `findAll({})` calls produce deeply-equal results (the determinism contract, D-04); a
    `squad_id` filter for the empty squad returns `items` as an empty array with `total` 0.
  </action>
  <acceptance_criteria>
    - `npm run build --workspace=apps/gutcallfun-core` exits 0
    - `npx jest src/modules/api/users` exits 0 with at least 4 passing assertions
    - `grep -c 'wallet_address' apps/gutcallfun-core/src/modules/api/users/dto/user-response.dto.ts` returns at least 1
    - `grep -c 'handle\|image' apps/gutcallfun-core/src/modules/api/users/dto/update-user.dto.ts` returns at least 2 and the file declares no other data property
    - `grep -c 'ParseUUIDPipe' apps/gutcallfun-core/src/modules/api/users/users.controller.ts` returns at least 1
    - `grep -c 'InjectRepository' apps/gutcallfun-core/src/modules/api/users/users.service.ts` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/api/users</automated>
  </verify>
  <done>Five user routes exist as compiled, Swagger-decorated handlers over a fixture-backed service, with unit specs proving determinism, the 404 path, and the empty-list path.</done>
</task>

<task type="auto">
  <name>Task 3: Squad DTOs, service, controller, module</name>
  <files>apps/gutcallfun-core/src/modules/api/squads/dto/squad-response.dto.ts, apps/gutcallfun-core/src/modules/api/squads/dto/create-squad.dto.ts, apps/gutcallfun-core/src/modules/api/squads/dto/list-squads-query.dto.ts, apps/gutcallfun-core/src/modules/api/squads/dto/squad-participant-response.dto.ts, apps/gutcallfun-core/src/modules/api/squads/dto/create-squad-participant.dto.ts, apps/gutcallfun-core/src/modules/api/squads/dto/squad-score-profile-response.dto.ts, apps/gutcallfun-core/src/modules/api/squads/squads.controller.ts, apps/gutcallfun-core/src/modules/api/squads/squads.service.ts, apps/gutcallfun-core/src/modules/api/squads/squads.service.spec.ts, apps/gutcallfun-core/src/modules/api/squads/squads.module.ts</files>
  <read_first>
    - initial-request-src/initial-db-structure.sql (tables `squad`, `squad_participant`, `squad_score_profile`)
    - apps/gutcallfun-core/src/models/squad/squad.entity.ts and squad-participant.entity.ts
    - apps/gutcallfun-core/src/mocks/fixtures/squads.fixtures.ts (created in Task 1)
    - apps/gutcallfun-core/src/modules/api/users/users.controller.ts (the controller conventions established in Task 2 — mirror them)
    - .planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/COVERAGE.md (the squad opt-out register — update and delete are deliberately absent)
  </read_first>
  <action>
    Create the DTOs under `src/modules/api/squads/dto/`, snake_case throughout, mirroring the
    conventions Task 2 established.

    - `SquadResponseDto`: `id` (`type: 'integer'`), `name`, `image`, `invite_code`, `active`,
      `created_at`, `updated_at`, `deleted_at`.
    - `CreateSquadDto`: `name` (required string), `image` and `invite_code` optional. The client
      does NOT supply `id` — the service assigns it via the `nextSquadId()` helper.
    - `ListSquadsQueryDto`: extends the shared pagination DTO; optional `participant_id`
      (`@IsUUID()`) filter meaning "squads this user belongs to".
    - `SquadParticipantResponseDto`: `squad_id` (`type: 'integer'`), `user_id`, `active`,
      `score_profile`, `created_at`, `deleted_at`. There is no surrogate id — the primary key is
      the composite `(squad_id, user_id)`, so do not invent an `id` property.
    - `CreateSquadParticipantDto`: `user_id` (`@IsUUID()`) only; `squad_id` comes from the path.
    - `SquadScoreProfileResponseDto`: `id`, `total_points` (`type: 'integer'`), `games_played`
      (`type: 'integer'`), `updated_at`.
    - Concrete paginated envelope subclasses for squads and for participants.

    Create `squads.service.ts` in the same fixture-backed style — no repository, no TypeORM
    import, logger instantiated as `new Logger(SquadsService.name)`. Methods: `create(dto)`
    assigning an explicit id from `nextSquadId()` and returning the full `SquadResponseDto`;
    `findOne(squadId)` throwing `NotFoundException` when absent; `findAll(query)` optionally
    filtered by `participant_id` through the participants fixture; `addParticipant(squadId, dto)`
    resolving the squad first (404 when absent) and returning the participant row shape;
    `findParticipants(squadId)` returning the paginated participant list, which is an empty array
    with total 0 for the deliberately-empty fixture squad; `findScoreProfile(squadId)` returning
    the profile referenced through `squad_participant.score_profile` — the FK points from the
    participant at the profile, not the other way round, so resolve in that direction rather than
    looking up a profile keyed by squad id.

    Create `squads.controller.ts` with `@Controller('squads')` and `@ApiTags('squads')`. Routes:
    `POST /squads`, `GET /squads`, `GET /squads/:squad_id`, `POST /squads/:squad_id/participants`,
    `GET /squads/:squad_id/participants`, `GET /squads/:squad_id/score-profile`. Bind `:squad_id`
    with `@Param('squad_id', ParseIntPipe)` because squad ids are integers, so a non-numeric
    segment fails with a 400 rather than reaching the handler. Do not add update or delete routes —
    both are recorded as deliberate opt-outs in COVERAGE.md.

    Create `squads.module.ts` in the same shape as `users.module.ts`, and
    `squads.service.spec.ts` asserting: `create` returns an id strictly greater than the highest
    fixture id and never undefined or null; `findOne` on an unknown id throws `NotFoundException`;
    `findParticipants` on the empty fixture squad returns an empty `items` array with `total` 0;
    two successive `findAll({})` calls are deeply equal.
  </action>
  <acceptance_criteria>
    - `npm run build --workspace=apps/gutcallfun-core` exits 0
    - `npx jest src/modules/api/squads` exits 0 with at least 4 passing assertions
    - `grep -c 'ParseIntPipe' apps/gutcallfun-core/src/modules/api/squads/squads.controller.ts` returns at least 1
    - `grep -cE '@(Patch|Delete)\(' apps/gutcallfun-core/src/modules/api/squads/squads.controller.ts` returns 0
    - `grep -c 'InjectRepository' apps/gutcallfun-core/src/modules/api/squads/squads.service.ts` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/gutcallfun-core && npm run build && npx jest src/modules/api/squads</automated>
  </verify>
  <done>Six squad routes compile with explicit id assignment on create, the reversed score-profile FK resolved in the correct direction, and no update/delete route present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → users/squads REST handlers | Untrusted path params, query params, and JSON bodies reach eleven new handlers. |
| fixture module → HTTP response | Whatever is committed into the fixture files is served publicly and unauthenticated from any deployed demo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-07 | Tampering | `PATCH /users/:user_id` body | high | mitigate | `UpdateUserDto` declares only `handle` and `image`. Combined with the `forbidNonWhitelisted: true` pipe from plan 01, a body carrying `wallet_address`, `id`, `share_code`, or `score_profile` is rejected with a 400 rather than silently applied. Acceptance criterion asserts the DTO declares no other data property. |
| T-02.1-08 | Information Disclosure | `users.fixtures.ts` committed content | high | mitigate | Fixtures must contain no real wallet address, handle, or avatar URL belonging to an actual person. Recorded as a prohibition on this plan; the file is world-readable in the repository and served unauthenticated. |
| T-02.1-09 | Denial of Service | `GET /users/:user_id`, `GET /squads/:squad_id` | medium | mitigate | `ParseUUIDPipe` and `ParseIntPipe` reject malformed path params with a 400 before the handler body executes, so a non-uuid or non-numeric segment cannot throw an unhandled exception and surface as a 500 (API-02). |
| T-02.1-10 | Denial of Service | `GET /users`, `GET /squads` list paging | medium | mitigate | Both inherit the shared `PaginationQueryDto` with its `@Max(100)` cap, so an unauthenticated caller cannot request an unbounded page. |
| T-02.1-11 | Spoofing | Every route in this plan | high | accept | D-02: no authentication exists, so any caller may read or write as any user. Accepted **only** because these handlers read static in-process fixtures — there is no persistence, no real user data, and no privileged action reachable. This acceptance expires the moment a repository is injected behind any of these methods; Phase 5 must land the Phase 3 session guard in the same change that adds persistence. |
| T-02.1-12 | Information Disclosure | `GET /users?squad_id=` teammate enumeration | low | accept | An unauthenticated caller can enumerate squad membership. Same rationale as T-02.1-11 — the membership graph is synthetic fixture data. Revisit with real data. |
</threat_model>

<verification>
1. `cd apps/gutcallfun-core && npm run build` exits 0.
2. `cd apps/gutcallfun-core && npx jest src/modules/api` exits 0.
3. `cd apps/gutcallfun-core && npm test` exits 0 — the 148 pre-existing unit tests must not regress.
4. No file under `src/modules/ingest/**` or `src/models/**` was modified: `git diff --name-only | grep -cE 'src/(modules/ingest|models)/'` returns 0.
5. Every response DTO property name appears verbatim as a column name in `initial-db-structure.sql`.
</verification>

<success_criteria>
- Eleven account/squad routes exist, Swagger-decorated, returning shaped non-empty deterministic mocks (D-01, D-04).
- Every DTO property is snake_case and matches `initial-db-structure.sql` 1:1, so Phase 5 swaps implementations behind identical DTOs (D-05).
- No repository injection, no TypeORM import, no auth guard anywhere in this plan (D-02).
- `squad.id` is assigned explicitly on create; no code path assumes a database default.
- Unit specs prove determinism, the 404 path, the 400 path, and the empty-list path.
</success_criteria>

<artifacts_this_phase_produces>
**New files:** all 21 paths listed in `files_modified`.

**New exported symbols**
- Fixtures: `USERS_FIXTURE`, `USER_SCORE_PROFILES_FIXTURE`, `SQUADS_FIXTURE`, `SQUAD_PARTICIPANTS_FIXTURE`, `SQUAD_SCORE_PROFILES_FIXTURE`, `nextSquadId`
- DTO classes: `UserResponseDto`, `CreateUserDto`, `UpdateUserDto`, `ListUsersQueryDto`, `UserScoreProfileResponseDto`, `PaginatedUsersResponseDto`, `SquadResponseDto`, `CreateSquadDto`, `ListSquadsQueryDto`, `SquadParticipantResponseDto`, `CreateSquadParticipantDto`, `SquadScoreProfileResponseDto`, `PaginatedSquadsResponseDto`, `PaginatedSquadParticipantsResponseDto`
- Services / controllers / modules: `UsersService`, `UsersController`, `UsersModule`, `SquadsService`, `SquadsController`, `SquadsModule`

**New HTTP routes**
- `GET /users`, `GET /users/:user_id`, `POST /users`, `PATCH /users/:user_id`, `GET /users/:user_id/score-profile`
- `POST /squads`, `GET /squads`, `GET /squads/:squad_id`, `POST /squads/:squad_id/participants`, `GET /squads/:squad_id/participants`, `GET /squads/:squad_id/score-profile`
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/backend/phases/02.1-placeholder-api-surface-fake-realtime-contract/02.1-02-SUMMARY.md` when done.
</output>
