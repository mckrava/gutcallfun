---
phase: 02.1-placeholder-api-surface-fake-realtime-contract
plan: 01
subsystem: api
tags: [nestjs, swagger, cors, validation-pipe, socket.io, class-validator, class-transformer, config]

# Dependency graph
requires:
  - phase: 01-foundation-data-layer
    provides: EnvironmentVariables fail-fast config schema and ConfigService-based access pattern
provides:
  - Five pinned npm packages (@nestjs/websockets, @nestjs/platform-socket.io, socket.io, @nestjs/swagger, socket.io-client) installed behind a completed human legitimacy checkpoint
  - WEB_APP_ORIGIN required, fail-fast env var (D-06)
  - main.ts bootstrap wiring: global ValidationPipe (whitelist+forbidNonWhitelisted+transform), single-origin CORS, Swagger UI at /api-docs
  - Shared PaginationQueryDto and abstract PaginatedResponseDto for plans 02/03 to extend
affects: [02.1-02, 02.1-03, 02.1-04, 02.1-05]

# Tech tracking
tech-stack:
  added: ["@nestjs/websockets@11.1.28", "@nestjs/platform-socket.io@11.1.28", "socket.io@4.8.3", "@nestjs/swagger@11.4.6", "socket.io-client@4.8.3 (dev)"]
  patterns: ["ConfigService.get() for all env access (never bare process.env, except the documented @WebSocketGateway cors exception reserved for a later plan)", "Abstract generic-shaped response DTO base class re-declared per resource in subclasses (Swagger cannot infer generics)"]

key-files:
  created:
    - apps/gutcallfun-core/src/common/dto/pagination-query.dto.ts
    - apps/gutcallfun-core/src/common/dto/paginated-response.dto.ts
  modified:
    - apps/gutcallfun-core/src/main.ts
    - apps/gutcallfun-core/src/config/app-config.schema.ts
    - apps/gutcallfun-core/src/config/app-config.schema.spec.ts
    - apps/gutcallfun-core/package.json
    - apps/gutcallfun-core/.env.example
    - package-lock.json

key-decisions:
  - "Package legitimacy checkpoint (Task 1) approved by human before any install: all three SUS-flagged packages confirmed to be the nestjs/socketio orgs' own lockstep-versioned releases, no postinstall scripts, peer-locked to the already-running @nestjs/common@11.1.28 major."
  - "@nestjs/swagger installed at 11.4.6 per the human's explicit version decision, even though .claude/CLAUDE.md's stack table still records 11.4.5 (one patch stale) — CLAUDE.md was NOT edited; drift recorded here for later reconciliation."
  - "app-config.schema.spec.ts already existed (from Phase 1); extended it with WEB_APP_ORIGIN assertions instead of creating a duplicate file, since the plan's premise that this file was new did not match the actual repo state."

requirements-completed: [API-01, API-02, WS-03]

coverage:
  - id: D1
    description: "Five packages (@nestjs/websockets, @nestjs/platform-socket.io, socket.io, @nestjs/swagger, socket.io-client) installed at exact pinned versions with typeorm's 0.3.31 pin unchanged"
    verification:
      - kind: other
        ref: "grep -c '\"typeorm\": \"0.3.31\"' apps/gutcallfun-core/package.json (returns 1); npm run build --workspace=apps/gutcallfun-core (exits 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "WEB_APP_ORIGIN (D-06) validated fail-fast: required, no default, @IsUrl(require_protocol: true), boots successfully only with a protocol-qualified value"
    requirement: "API-01"
    verification:
      - kind: unit
        ref: "apps/gutcallfun-core/src/config/app-config.schema.spec.ts — Test 12/13/14 (WEB_APP_ORIGIN absent/protocol-less/valid)"
        status: pass
    human_judgment: false
  - id: D3
    description: "main.ts bootstrap wiring: single-origin CORS (never wildcard), global ValidationPipe (whitelist+forbidNonWhitelisted+transform), Swagger mounted at /api-docs with no credentials in the description"
    requirement: "API-01"
    verification:
      - kind: integration
        ref: "Live app boot: GET /api-docs -> 200, GET /api-docs-json -> 200 valid OpenAPI 3 doc, Origin: http://localhost:3001 echoed exactly in Access-Control-Allow-Origin, no txline/jwt/api_token/txoracle string present in served document"
        status: pass
    human_judgment: false
  - id: D4
    description: "Shared PaginationQueryDto (limit 1-100 default 20, offset >=0 default 0) and abstract PaginatedResponseDto (total/limit/offset, items per-subclass) documented with explicit OpenAPI type: 'integer'"
    requirement: "API-02"
    verification:
      - kind: other
        ref: "npm run build --workspace=apps/gutcallfun-core (exits 0, decorators compile)"
        status: pass
    human_judgment: true
    rationale: "No dedicated unit/e2e test exercises these DTOs in this plan (no controller consumes them yet — that's plans 02/03). Only compile-time validation was performed here; first real exercise happens when a concrete list endpoint extends PaginatedResponseDto and applies PaginationQueryDto to a live query param."

duration: 16min
completed: 2026-07-18
status: complete
---

# Phase 02.1 Plan 01: Bootstrap & Shared Contracts Summary

**Installed Swagger/socket.io deps behind an approved package-legitimacy gate, wired global ValidationPipe + single-origin CORS + Swagger UI into a previously 9-line main.ts, added WEB_APP_ORIGIN fail-fast config (D-06), and shipped the shared PaginationQueryDto/PaginatedResponseDto contract plans 02-03 will reuse.**

## Performance

- **Duration:** 16 min (this continuation session, resuming past a pre-cleared human checkpoint)
- **Started:** 2026-07-18T13:25:31Z (base commit bab06f5)
- **Completed:** 2026-07-18T13:41:22Z
- **Tasks:** 3 (Task 1 checkpoint cleared by a prior agent + the user before this session)
- **Files modified:** 8 (5 modified + 2 created in Task 2/3, plus package-lock.json)

## Accomplishments
- Five packages (`@nestjs/websockets`, `@nestjs/platform-socket.io`, `socket.io`, `@nestjs/swagger`, `socket.io-client`) installed at exact pinned versions after a human-approved package legitimacy review; `typeorm@0.3.31` confirmed unchanged
- `WEB_APP_ORIGIN` added to `EnvironmentVariables` as a required, no-default, `@IsUrl(require_protocol: true)` field (D-06), with 3 new fail-fast unit assertions
- `main.ts` now wires a single-origin CORS policy (`credentials: false`, never a wildcard), a global `ValidationPipe({ whitelist, transform, forbidNonWhitelisted })`, and `SwaggerModule` mounted at `/api-docs` — verified live against a real Postgres-backed boot, not just a build check
- Shared `PaginationQueryDto` and abstract `PaginatedResponseDto<T>` created with explicit `type: 'integer'` Swagger annotations for every plan-02/03 list endpoint to reuse

## Task Commits

Each task was committed atomically:

1. **Task 1: Package legitimacy review** — pre-cleared by the orchestrator/human before this session; no commit (checkpoint made no file changes by design)
2. **Task 2: Install packages + WEB_APP_ORIGIN fail-fast config** - `50d22bb` (feat)
3. **Task 3: Bootstrap wiring — ValidationPipe, CORS, Swagger, shared pagination DTOs** - `d5c3a0b` (feat)

_This is a worktree-isolated execution; the orchestrator applies the plan-metadata commit (docs) after merging this wave, per the parallel-execution contract._

## Files Created/Modified
- `apps/gutcallfun-core/src/common/dto/pagination-query.dto.ts` - Shared `limit`/`offset` query DTO (1-100 default 20 / >=0 default 0), explicit integer Swagger types
- `apps/gutcallfun-core/src/common/dto/paginated-response.dto.ts` - Abstract `{ items, total, limit, offset }` envelope base class
- `apps/gutcallfun-core/src/main.ts` - Global `ValidationPipe`, single-origin CORS via `ConfigService`, Swagger `DocumentBuilder`/`SwaggerModule.setup('api-docs', ...)`, `enableShutdownHooks()`/`PORT ?? 3000` preserved
- `apps/gutcallfun-core/src/config/app-config.schema.ts` - Added `WEB_APP_ORIGIN` fail-fast field
- `apps/gutcallfun-core/src/config/app-config.schema.spec.ts` - Extended (pre-existing file) with `WEB_APP_ORIGIN` fixture value + 3 new fail-fast tests
- `apps/gutcallfun-core/package.json` / `package-lock.json` - Five new pinned dependencies
- `apps/gutcallfun-core/.env.example` - Added `WEB_APP_ORIGIN=http://localhost:3001`

## Decisions Made
- Human approved the Task 1 package legitimacy checkpoint: all three `SUS`-flagged packages (`@nestjs/websockets`, `@nestjs/platform-socket.io`, `@nestjs/swagger`) confirmed to be genuine `nestjs` org releases sharing the monorepo's lockstep version cadence, peer-locked to the already-installed `@nestjs/common@11.1.28` major, with no `postinstall` scripts.
- Installed `@nestjs/swagger@11.4.6` (current npm `latest`) per the human's explicit instruction, even though `.claude/CLAUDE.md`'s stack table still records `11.4.5`. **Deliberately did not edit CLAUDE.md** — recording this one-patch drift here for later reconciliation rather than "correcting" either document unilaterally.
- `src/config/app-config.schema.spec.ts` already existed on disk (committed in Phase 1/prior 02 work) with 11 pre-existing tests; the plan's frontmatter listed it as a file this plan "creates." Rather than overwrite it (which would have deleted 11 passing tests), extended it in place with `WEB_APP_ORIGIN` fixture coverage and 3 new assertions — net result matches the plan's intent (WEB_APP_ORIGIN fail-fast contract proven) without destroying existing coverage.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree had no local `.env` file to boot the app against**
- **Found during:** Task 2/3 verification (running the app + full test suite)
- **Issue:** This git worktree is isolated from the main checkout's gitignored files; `apps/gutcallfun-core/.env` did not exist here at all (not just missing `WEB_APP_ORIGIN`), which would have failed every test/build step that boots `AppModule` regardless of this plan's changes, and made the plan's own `<verification>` step 4-6 (live app checks) impossible to run.
- **Fix:** Copied the main checkout's real `.env` into this worktree (itself gitignored, never staged/committed) and appended `WEB_APP_ORIGIN=http://localhost:3001`, matching the plan's Task 2 instruction to "add the same line to the developer's local `.env`."
- **Files modified:** `apps/gutcallfun-core/.env` (untracked, gitignored — confirmed via `git check-ignore -v`; not staged, not committed, not part of any diff)
- **Verification:** `git status --short` after each commit shows no `.env` entry; a running Postgres 16 container was already available on `127.0.0.1:5488` (root `docker-compose.yml`), so the app booted against it successfully for the live curl checks below.

**2. [Rule 1 - Bug] Own code comment created a false-positive grep match**
- **Found during:** Task 3, running the plan's own acceptance-criteria grep for `forbidNonWhitelisted`
- **Issue:** My first draft of `main.ts` included an explanatory comment that repeated the literal string `forbidNonWhitelisted`, making the acceptance grep return 2 instead of the required 1.
- **Fix:** Reworded the comment to describe the same behavior without repeating the flag name literally.
- **Files modified:** `apps/gutcallfun-core/src/main.ts`
- **Verification:** `grep -c 'forbidNonWhitelisted' apps/gutcallfun-core/src/main.ts` now returns 1; re-ran `npm run build` to confirm no regression.
- **Committed in:** `d5c3a0b` (Task 3 commit — fixed before commit, not a separate commit)

---

**Total deviations:** 2 auto-fixed (1 blocking-environmental, 1 self-caught bug)
**Impact on plan:** Neither deviation changed the plan's design or scope. The `.env` gap is a worktree-isolation artifact (secrets are gitignored by design per CLAUDE.md's security constraint) rather than anything wrong with the plan itself; no secret was echoed, logged, or committed anywhere in this session, and the copied `.env` remains untracked exactly as before.

## Issues Encountered
- `npm run start:prod` failed with `MODULE_NOT_FOUND` for `dist/main` — this Nest CLI build actually emits to `dist/src/main.js` (not `dist/main.js`) in this project's `tsconfig`/Nest CLI configuration. Not a regression from this plan (the `start:prod` script itself is pre-existing and untouched); ran `node dist/src/main.js` directly to perform the live verification instead. Not filed as a fix because the `package.json` script itself is out of scope for this plan (no task references `start:prod`), but worth flagging: `npm run start:prod` as currently defined would not work as-is post-build. Logged to `deferred-items.md`.

## User Setup Required

None - no external service configuration required. `WEB_APP_ORIGIN` is a local-origin string set entirely by the developer's own `.env` (per the plan's `user_setup` frontmatter), not a third-party credential.

## Next Phase Readiness
- `@nestjs/swagger` decorators, `PaginationQueryDto`, and `PaginatedResponseDto` are now available for plans 02.1-02 and 02.1-03 to import into their mock resource controllers.
- `main.ts` boots cleanly, refuses to boot without a protocol-qualified `WEB_APP_ORIGIN`, serves Swagger UI, and rejects undeclared request properties with a 400 — the foundation this whole phase's remaining plans depend on is in place.
- 151 unit tests + 2 e2e tests all green (148 pre-existing + 3 new `WEB_APP_ORIGIN` assertions, net of the file that was extended rather than newly created).
- Flag for a human/orchestrator to reconcile: `.claude/CLAUDE.md`'s stack table still lists `@nestjs/swagger` as `11.4.5`; the repo now runs `11.4.6` per this plan's approved checkpoint.
- Flag for a human/orchestrator: `apps/gutcallfun-core/package.json`'s `start:prod` script (`node dist/main`) does not match the Nest CLI's actual `dist/src/main.js` output path — pre-existing, out of scope here, but will block a real deploy attempt until corrected.

---
*Phase: 02.1-placeholder-api-surface-fake-realtime-contract*
*Completed: 2026-07-18*

## Self-Check: PASSED

All created/modified files confirmed present on disk; both task commits (`50d22bb`, `d5c3a0b`) confirmed present in `git log --oneline --all`.
