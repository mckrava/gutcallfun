// Entry route. The controller (in the root layout) mirrors engine state to the
// URL, so on mount it redirects here to /signin (or /matches once authed).
export default function Page() {
  return null;
}
